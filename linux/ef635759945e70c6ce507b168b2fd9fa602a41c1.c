// KASAN: use-after-free Read in j1939_sk_recv
// https://syzkaller.appspot.com/bug?id=ef635759945e70c6ce507b168b2fd9fa602a41c1
// status:fixed
// autogenerated by syzkaller (https://github.com/google/syzkaller)

#define _GNU_SOURCE

#include <arpa/inet.h>
#include <dirent.h>
#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <net/if.h>
#include <net/if_arp.h>
#include <netinet/in.h>
#include <pthread.h>
#include <sched.h>
#include <setjmp.h>
#include <signal.h>
#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/ioctl.h>
#include <sys/mount.h>
#include <sys/prctl.h>
#include <sys/resource.h>
#include <sys/socket.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/time.h>
#include <sys/types.h>
#include <sys/uio.h>
#include <sys/wait.h>
#include <time.h>
#include <unistd.h>

#include <linux/capability.h>
#include <linux/futex.h>
#include <linux/genetlink.h>
#include <linux/if_addr.h>
#include <linux/if_ether.h>
#include <linux/if_link.h>
#include <linux/if_tun.h>
#include <linux/in6.h>
#include <linux/ip.h>
#include <linux/neighbour.h>
#include <linux/net.h>
#include <linux/netlink.h>
#include <linux/rtnetlink.h>
#include <linux/tcp.h>
#include <linux/veth.h>

unsigned long long procid;

static __thread int skip_segv;
static __thread jmp_buf segv_env;

static void segv_handler(int sig, siginfo_t* info, void* ctx)
{
  uintptr_t addr = (uintptr_t)info->si_addr;
  const uintptr_t prog_start = 1 << 20;
  const uintptr_t prog_end = 100 << 20;
  if (__atomic_load_n(&skip_segv, __ATOMIC_RELAXED) &&
      (addr < prog_start || addr > prog_end)) {
    _longjmp(segv_env, 1);
  }
  exit(sig);
}

static void install_segv_handler(void)
{
  struct sigaction sa;
  memset(&sa, 0, sizeof(sa));
  sa.sa_handler = SIG_IGN;
  syscall(SYS_rt_sigaction, 0x20, &sa, NULL, 8);
  syscall(SYS_rt_sigaction, 0x21, &sa, NULL, 8);
  memset(&sa, 0, sizeof(sa));
  sa.sa_sigaction = segv_handler;
  sa.sa_flags = SA_NODEFER | SA_SIGINFO;
  sigaction(SIGSEGV, &sa, NULL);
  sigaction(SIGBUS, &sa, NULL);
}

#define NONFAILING(...)                                                        \
  {                                                                            \
    __atomic_fetch_add(&skip_segv, 1, __ATOMIC_SEQ_CST);                       \
    if (_setjmp(segv_env) == 0) {                                              \
      __VA_ARGS__;                                                             \
    }                                                                          \
    __atomic_fetch_sub(&skip_segv, 1, __ATOMIC_SEQ_CST);                       \
  }

static void sleep_ms(uint64_t ms)
{
  usleep(ms * 1000);
}

static uint64_t current_time_ms(void)
{
  struct timespec ts;
  if (clock_gettime(CLOCK_MONOTONIC, &ts))
    exit(1);
  return (uint64_t)ts.tv_sec * 1000 + (uint64_t)ts.tv_nsec / 1000000;
}

static void thread_start(void* (*fn)(void*), void* arg)
{
  pthread_t th;
  pthread_attr_t attr;
  pthread_attr_init(&attr);
  pthread_attr_setstacksize(&attr, 128 << 10);
  int i;
  for (i = 0; i < 100; i++) {
    if (pthread_create(&th, &attr, fn, arg) == 0) {
      pthread_attr_destroy(&attr);
      return;
    }
    if (errno == EAGAIN) {
      usleep(50);
      continue;
    }
    break;
  }
  exit(1);
}

#define BITMASK(bf_off, bf_len) (((1ull << (bf_len)) - 1) << (bf_off))
#define STORE_BY_BITMASK(type, htobe, addr, val, bf_off, bf_len)               \
  *(type*)(addr) =                                                             \
      htobe((htobe(*(type*)(addr)) & ~BITMASK((bf_off), (bf_len))) |           \
            (((type)(val) << (bf_off)) & BITMASK((bf_off), (bf_len))))

typedef struct {
  int state;
} event_t;

static void event_init(event_t* ev)
{
  ev->state = 0;
}

static void event_reset(event_t* ev)
{
  ev->state = 0;
}

static void event_set(event_t* ev)
{
  if (ev->state)
    exit(1);
  __atomic_store_n(&ev->state, 1, __ATOMIC_RELEASE);
  syscall(SYS_futex, &ev->state, FUTEX_WAKE | FUTEX_PRIVATE_FLAG);
}

static void event_wait(event_t* ev)
{
  while (!__atomic_load_n(&ev->state, __ATOMIC_ACQUIRE))
    syscall(SYS_futex, &ev->state, FUTEX_WAIT | FUTEX_PRIVATE_FLAG, 0, 0);
}

static int event_isset(event_t* ev)
{
  return __atomic_load_n(&ev->state, __ATOMIC_ACQUIRE);
}

static int event_timedwait(event_t* ev, uint64_t timeout)
{
  uint64_t start = current_time_ms();
  uint64_t now = start;
  for (;;) {
    uint64_t remain = timeout - (now - start);
    struct timespec ts;
    ts.tv_sec = remain / 1000;
    ts.tv_nsec = (remain % 1000) * 1000 * 1000;
    syscall(SYS_futex, &ev->state, FUTEX_WAIT | FUTEX_PRIVATE_FLAG, 0, &ts);
    if (__atomic_load_n(&ev->state, __ATOMIC_RELAXED))
      return 1;
    now = current_time_ms();
    if (now - start > timeout)
      return 0;
  }
}

static bool write_file(const char* file, const char* what, ...)
{
  char buf[1024];
  va_list args;
  va_start(args, what);
  vsnprintf(buf, sizeof(buf), what, args);
  va_end(args);
  buf[sizeof(buf) - 1] = 0;
  int len = strlen(buf);
  int fd = open(file, O_WRONLY | O_CLOEXEC);
  if (fd == -1)
    return false;
  if (write(fd, buf, len) != len) {
    int err = errno;
    close(fd);
    errno = err;
    return false;
  }
  close(fd);
  return true;
}

static struct {
  char* pos;
  int nesting;
  struct nlattr* nested[8];
  char buf[1024];
} nlmsg;

static void netlink_init(int typ, int flags, const void* data, int size)
{
  memset(&nlmsg, 0, sizeof(nlmsg));
  struct nlmsghdr* hdr = (struct nlmsghdr*)nlmsg.buf;
  hdr->nlmsg_type = typ;
  hdr->nlmsg_flags = NLM_F_REQUEST | NLM_F_ACK | flags;
  memcpy(hdr + 1, data, size);
  nlmsg.pos = (char*)(hdr + 1) + NLMSG_ALIGN(size);
}

static void netlink_attr(int typ, const void* data, int size)
{
  struct nlattr* attr = (struct nlattr*)nlmsg.pos;
  attr->nla_len = sizeof(*attr) + size;
  attr->nla_type = typ;
  memcpy(attr + 1, data, size);
  nlmsg.pos += NLMSG_ALIGN(attr->nla_len);
}

static void netlink_nest(int typ)
{
  struct nlattr* attr = (struct nlattr*)nlmsg.pos;
  attr->nla_type = typ;
  nlmsg.pos += sizeof(*attr);
  nlmsg.nested[nlmsg.nesting++] = attr;
}

static void netlink_done(void)
{
  struct nlattr* attr = nlmsg.nested[--nlmsg.nesting];
  attr->nla_len = nlmsg.pos - (char*)attr;
}

static int netlink_send_ext(int sock, uint16_t reply_type, int* reply_len)
{
  if (nlmsg.pos > nlmsg.buf + sizeof(nlmsg.buf) || nlmsg.nesting)
    exit(1);
  struct nlmsghdr* hdr = (struct nlmsghdr*)nlmsg.buf;
  hdr->nlmsg_len = nlmsg.pos - nlmsg.buf;
  struct sockaddr_nl addr;
  memset(&addr, 0, sizeof(addr));
  addr.nl_family = AF_NETLINK;
  unsigned n = sendto(sock, nlmsg.buf, hdr->nlmsg_len, 0,
                      (struct sockaddr*)&addr, sizeof(addr));
  if (n != hdr->nlmsg_len)
    exit(1);
  n = recv(sock, nlmsg.buf, sizeof(nlmsg.buf), 0);
  if (n < sizeof(struct nlmsghdr))
    exit(1);
  if (reply_len && hdr->nlmsg_type == reply_type) {
    *reply_len = n;
    return 0;
  }
  if (n < sizeof(struct nlmsghdr) + sizeof(struct nlmsgerr))
    exit(1);
  if (hdr->nlmsg_type != NLMSG_ERROR)
    exit(1);
  return -((struct nlmsgerr*)(hdr + 1))->error;
}

static int netlink_send(int sock)
{
  return netlink_send_ext(sock, 0, NULL);
}

static void netlink_add_device_impl(const char* type, const char* name)
{
  struct ifinfomsg hdr;
  memset(&hdr, 0, sizeof(hdr));
  netlink_init(RTM_NEWLINK, NLM_F_EXCL | NLM_F_CREATE, &hdr, sizeof(hdr));
  if (name)
    netlink_attr(IFLA_IFNAME, name, strlen(name));
  netlink_nest(IFLA_LINKINFO);
  netlink_attr(IFLA_INFO_KIND, type, strlen(type));
}

static void netlink_add_device(int sock, const char* type, const char* name)
{
  netlink_add_device_impl(type, name);
  netlink_done();
  int err = netlink_send(sock);
  (void)err;
}

static void netlink_add_veth(int sock, const char* name, const char* peer)
{
  netlink_add_device_impl("veth", name);
  netlink_nest(IFLA_INFO_DATA);
  netlink_nest(VETH_INFO_PEER);
  nlmsg.pos += sizeof(struct ifinfomsg);
  netlink_attr(IFLA_IFNAME, peer, strlen(peer));
  netlink_done();
  netlink_done();
  netlink_done();
  int err = netlink_send(sock);
  (void)err;
}

static void netlink_add_hsr(int sock, const char* name, const char* slave1,
                            const char* slave2)
{
  netlink_add_device_impl("hsr", name);
  netlink_nest(IFLA_INFO_DATA);
  int ifindex1 = if_nametoindex(slave1);
  netlink_attr(IFLA_HSR_SLAVE1, &ifindex1, sizeof(ifindex1));
  int ifindex2 = if_nametoindex(slave2);
  netlink_attr(IFLA_HSR_SLAVE2, &ifindex2, sizeof(ifindex2));
  netlink_done();
  netlink_done();
  int err = netlink_send(sock);
  (void)err;
}

static void netlink_device_change(int sock, const char* name, bool up,
                                  const char* master, const void* mac,
                                  int macsize)
{
  struct ifinfomsg hdr;
  memset(&hdr, 0, sizeof(hdr));
  if (up)
    hdr.ifi_flags = hdr.ifi_change = IFF_UP;
  netlink_init(RTM_NEWLINK, 0, &hdr, sizeof(hdr));
  netlink_attr(IFLA_IFNAME, name, strlen(name));
  if (master) {
    int ifindex = if_nametoindex(master);
    netlink_attr(IFLA_MASTER, &ifindex, sizeof(ifindex));
  }
  if (macsize)
    netlink_attr(IFLA_ADDRESS, mac, macsize);
  int err = netlink_send(sock);
  (void)err;
}

static int netlink_add_addr(int sock, const char* dev, const void* addr,
                            int addrsize)
{
  struct ifaddrmsg hdr;
  memset(&hdr, 0, sizeof(hdr));
  hdr.ifa_family = addrsize == 4 ? AF_INET : AF_INET6;
  hdr.ifa_prefixlen = addrsize == 4 ? 24 : 120;
  hdr.ifa_scope = RT_SCOPE_UNIVERSE;
  hdr.ifa_index = if_nametoindex(dev);
  netlink_init(RTM_NEWADDR, NLM_F_CREATE | NLM_F_REPLACE, &hdr, sizeof(hdr));
  netlink_attr(IFA_LOCAL, addr, addrsize);
  netlink_attr(IFA_ADDRESS, addr, addrsize);
  return netlink_send(sock);
}

static void netlink_add_addr4(int sock, const char* dev, const char* addr)
{
  struct in_addr in_addr;
  inet_pton(AF_INET, addr, &in_addr);
  int err = netlink_add_addr(sock, dev, &in_addr, sizeof(in_addr));
  (void)err;
}

static void netlink_add_addr6(int sock, const char* dev, const char* addr)
{
  struct in6_addr in6_addr;
  inet_pton(AF_INET6, addr, &in6_addr);
  int err = netlink_add_addr(sock, dev, &in6_addr, sizeof(in6_addr));
  (void)err;
}

const int kInitNetNsFd = 239;

#define DEVLINK_FAMILY_NAME "devlink"

#define DEVLINK_CMD_RELOAD 37
#define DEVLINK_ATTR_BUS_NAME 1
#define DEVLINK_ATTR_DEV_NAME 2
#define DEVLINK_ATTR_NETNS_FD 137

static void netlink_devlink_netns_move(const char* bus_name,
                                       const char* dev_name, int netns_fd)
{
  struct genlmsghdr genlhdr;
  struct nlattr* attr;
  int sock, err, n;
  uint16_t id = 0;
  sock = socket(AF_NETLINK, SOCK_RAW, NETLINK_GENERIC);
  if (sock == -1)
    exit(1);
  memset(&genlhdr, 0, sizeof(genlhdr));
  genlhdr.cmd = CTRL_CMD_GETFAMILY;
  netlink_init(GENL_ID_CTRL, 0, &genlhdr, sizeof(genlhdr));
  netlink_attr(CTRL_ATTR_FAMILY_NAME, DEVLINK_FAMILY_NAME,
               strlen(DEVLINK_FAMILY_NAME) + 1);
  err = netlink_send_ext(sock, GENL_ID_CTRL, &n);
  if (err) {
    goto error;
  }
  attr =
      (struct nlattr*)(nlmsg.buf + NLMSG_HDRLEN + NLMSG_ALIGN(sizeof(genlhdr)));
  for (; (char*)attr < nlmsg.buf + n;
       attr = (struct nlattr*)((char*)attr + NLMSG_ALIGN(attr->nla_len))) {
    if (attr->nla_type == CTRL_ATTR_FAMILY_ID) {
      id = *(uint16_t*)(attr + 1);
      break;
    }
  }
  if (!id) {
    goto error;
  }
  recv(sock, nlmsg.buf, sizeof(nlmsg.buf), 0); /* recv ack */
  memset(&genlhdr, 0, sizeof(genlhdr));
  genlhdr.cmd = DEVLINK_CMD_RELOAD;
  netlink_init(id, 0, &genlhdr, sizeof(genlhdr));
  netlink_attr(DEVLINK_ATTR_BUS_NAME, bus_name, strlen(bus_name) + 1);
  netlink_attr(DEVLINK_ATTR_DEV_NAME, dev_name, strlen(dev_name) + 1);
  netlink_attr(DEVLINK_ATTR_NETNS_FD, &netns_fd, sizeof(netns_fd));
  netlink_send(sock);
error:
  close(sock);
}

static void initialize_devlink_pci(void)
{
  int netns = open("/proc/self/ns/net", O_RDONLY);
  if (netns == -1)
    exit(1);
  int ret = setns(kInitNetNsFd, 0);
  if (ret == -1)
    exit(1);
  netlink_devlink_netns_move("pci", "0000:00:10.0", netns);
  ret = setns(netns, 0);
  if (ret == -1)
    exit(1);
  close(netns);
}

#define DEV_IPV4 "172.20.20.%d"
#define DEV_IPV6 "fe80::%02x"
#define DEV_MAC 0x00aaaaaaaaaa

static void netdevsim_add(unsigned int addr, unsigned int port_count)
{
  char buf[16];
  sprintf(buf, "%u %u", addr, port_count);
  write_file("/sys/bus/netdevsim/new_device", buf);
}
static void initialize_netdevices(void)
{
  char netdevsim[16];
  sprintf(netdevsim, "netdevsim%d", (int)procid);
  struct {
    const char* type;
    const char* dev;
  } devtypes[] = {
      {"ip6gretap", "ip6gretap0"}, {"bridge", "bridge0"},
      {"vcan", "vcan0"},           {"bond", "bond0"},
      {"team", "team0"},           {"dummy", "dummy0"},
      {"nlmon", "nlmon0"},         {"caif", "caif0"},
      {"batadv", "batadv0"},       {"vxcan", "vxcan1"},
      {"netdevsim", netdevsim},    {"veth", 0},
  };
  const char* devmasters[] = {"bridge", "bond", "team"};
  struct {
    const char* name;
    int macsize;
    bool noipv6;
  } devices[] = {
      {"lo", ETH_ALEN},
      {"sit0", 0},
      {"bridge0", ETH_ALEN},
      {"vcan0", 0, true},
      {"tunl0", 0},
      {"gre0", 0},
      {"gretap0", ETH_ALEN},
      {"ip_vti0", 0},
      {"ip6_vti0", 0},
      {"ip6tnl0", 0},
      {"ip6gre0", 0},
      {"ip6gretap0", ETH_ALEN},
      {"erspan0", ETH_ALEN},
      {"bond0", ETH_ALEN},
      {"veth0", ETH_ALEN},
      {"veth1", ETH_ALEN},
      {"team0", ETH_ALEN},
      {"veth0_to_bridge", ETH_ALEN},
      {"veth1_to_bridge", ETH_ALEN},
      {"veth0_to_bond", ETH_ALEN},
      {"veth1_to_bond", ETH_ALEN},
      {"veth0_to_team", ETH_ALEN},
      {"veth1_to_team", ETH_ALEN},
      {"veth0_to_hsr", ETH_ALEN},
      {"veth1_to_hsr", ETH_ALEN},
      {"hsr0", 0},
      {"dummy0", ETH_ALEN},
      {"nlmon0", 0},
      {"vxcan0", 0, true},
      {"vxcan1", 0, true},
      {"caif0", ETH_ALEN},
      {"batadv0", ETH_ALEN},
      {netdevsim, ETH_ALEN},
  };
  int sock = socket(AF_NETLINK, SOCK_RAW, NETLINK_ROUTE);
  if (sock == -1)
    exit(1);
  unsigned i;
  for (i = 0; i < sizeof(devtypes) / sizeof(devtypes[0]); i++)
    netlink_add_device(sock, devtypes[i].type, devtypes[i].dev);
  for (i = 0; i < sizeof(devmasters) / (sizeof(devmasters[0])); i++) {
    char master[32], slave0[32], veth0[32], slave1[32], veth1[32];
    sprintf(slave0, "%s_slave_0", devmasters[i]);
    sprintf(veth0, "veth0_to_%s", devmasters[i]);
    netlink_add_veth(sock, slave0, veth0);
    sprintf(slave1, "%s_slave_1", devmasters[i]);
    sprintf(veth1, "veth1_to_%s", devmasters[i]);
    netlink_add_veth(sock, slave1, veth1);
    sprintf(master, "%s0", devmasters[i]);
    netlink_device_change(sock, slave0, false, master, 0, 0);
    netlink_device_change(sock, slave1, false, master, 0, 0);
  }
  netlink_device_change(sock, "bridge_slave_0", true, 0, 0, 0);
  netlink_device_change(sock, "bridge_slave_1", true, 0, 0, 0);
  netlink_add_veth(sock, "hsr_slave_0", "veth0_to_hsr");
  netlink_add_veth(sock, "hsr_slave_1", "veth1_to_hsr");
  netlink_add_hsr(sock, "hsr0", "hsr_slave_0", "hsr_slave_1");
  netlink_device_change(sock, "hsr_slave_0", true, 0, 0, 0);
  netlink_device_change(sock, "hsr_slave_1", true, 0, 0, 0);
  netdevsim_add((int)procid, 4);
  for (i = 0; i < sizeof(devices) / (sizeof(devices[0])); i++) {
    char addr[32];
    sprintf(addr, DEV_IPV4, i + 10);
    netlink_add_addr4(sock, devices[i].name, addr);
    if (!devices[i].noipv6) {
      sprintf(addr, DEV_IPV6, i + 10);
      netlink_add_addr6(sock, devices[i].name, addr);
    }
    uint64_t macaddr = DEV_MAC + ((i + 10ull) << 40);
    netlink_device_change(sock, devices[i].name, true, 0, &macaddr,
                          devices[i].macsize);
  }
  close(sock);
}
static void initialize_netdevices_init(void)
{
  int sock = socket(AF_NETLINK, SOCK_RAW, NETLINK_ROUTE);
  if (sock == -1)
    exit(1);
  struct {
    const char* type;
    int macsize;
    bool noipv6;
    bool noup;
  } devtypes[] = {
      {"nr", 7, true}, {"rose", 5, true, true},
  };
  unsigned i;
  for (i = 0; i < sizeof(devtypes) / sizeof(devtypes[0]); i++) {
    char dev[32], addr[32];
    sprintf(dev, "%s%d", devtypes[i].type, (int)procid);
    sprintf(addr, "172.30.%d.%d", i, (int)procid + 1);
    netlink_add_addr4(sock, dev, addr);
    if (!devtypes[i].noipv6) {
      sprintf(addr, "fe88::%02x:%02x", i, (int)procid + 1);
      netlink_add_addr6(sock, dev, addr);
    }
    int macsize = devtypes[i].macsize;
    uint64_t macaddr = 0xbbbbbb +
                       ((unsigned long long)i << (8 * (macsize - 2))) +
                       (procid << (8 * (macsize - 1)));
    netlink_device_change(sock, dev, !devtypes[i].noup, 0, &macaddr, macsize);
  }
  close(sock);
}

#define MAX_FDS 30

static long syz_open_dev(volatile long a0, volatile long a1, volatile long a2)
{
  if (a0 == 0xc || a0 == 0xb) {
    char buf[128];
    sprintf(buf, "/dev/%s/%d:%d", a0 == 0xc ? "char" : "block", (uint8_t)a1,
            (uint8_t)a2);
    return open(buf, O_RDWR, 0);
  } else {
    char buf[1024];
    char* hash;
    NONFAILING(strncpy(buf, (char*)a0, sizeof(buf) - 1));
    buf[sizeof(buf) - 1] = 0;
    while ((hash = strchr(buf, '#'))) {
      *hash = '0' + (char)(a1 % 10);
      a1 /= 10;
    }
    return open(buf, a2, 0);
  }
}

static void setup_common()
{
  if (mount(0, "/sys/fs/fuse/connections", "fusectl", 0, 0)) {
  }
}

static void loop();

static void sandbox_common()
{
  prctl(PR_SET_PDEATHSIG, SIGKILL, 0, 0, 0);
  setpgrp();
  setsid();
  int netns = open("/proc/self/ns/net", O_RDONLY);
  if (netns == -1)
    exit(1);
  if (dup2(netns, kInitNetNsFd) < 0)
    exit(1);
  close(netns);
  struct rlimit rlim;
  rlim.rlim_cur = rlim.rlim_max = (200 << 20);
  setrlimit(RLIMIT_AS, &rlim);
  rlim.rlim_cur = rlim.rlim_max = 32 << 20;
  setrlimit(RLIMIT_MEMLOCK, &rlim);
  rlim.rlim_cur = rlim.rlim_max = 136 << 20;
  setrlimit(RLIMIT_FSIZE, &rlim);
  rlim.rlim_cur = rlim.rlim_max = 1 << 20;
  setrlimit(RLIMIT_STACK, &rlim);
  rlim.rlim_cur = rlim.rlim_max = 0;
  setrlimit(RLIMIT_CORE, &rlim);
  rlim.rlim_cur = rlim.rlim_max = 256;
  setrlimit(RLIMIT_NOFILE, &rlim);
  if (unshare(CLONE_NEWNS)) {
  }
  if (unshare(CLONE_NEWIPC)) {
  }
  if (unshare(0x02000000)) {
  }
  if (unshare(CLONE_NEWUTS)) {
  }
  if (unshare(CLONE_SYSVSEM)) {
  }
  typedef struct {
    const char* name;
    const char* value;
  } sysctl_t;
  static const sysctl_t sysctls[] = {
      {"/proc/sys/kernel/shmmax", "16777216"},
      {"/proc/sys/kernel/shmall", "536870912"},
      {"/proc/sys/kernel/shmmni", "1024"},
      {"/proc/sys/kernel/msgmax", "8192"},
      {"/proc/sys/kernel/msgmni", "1024"},
      {"/proc/sys/kernel/msgmnb", "1024"},
      {"/proc/sys/kernel/sem", "1024 1048576 500 1024"},
  };
  unsigned i;
  for (i = 0; i < sizeof(sysctls) / sizeof(sysctls[0]); i++)
    write_file(sysctls[i].name, sysctls[i].value);
}

int wait_for_loop(int pid)
{
  if (pid < 0)
    exit(1);
  int status = 0;
  while (waitpid(-1, &status, __WALL) != pid) {
  }
  return WEXITSTATUS(status);
}

static void drop_caps(void)
{
  struct __user_cap_header_struct cap_hdr = {};
  struct __user_cap_data_struct cap_data[2] = {};
  cap_hdr.version = _LINUX_CAPABILITY_VERSION_3;
  cap_hdr.pid = getpid();
  if (syscall(SYS_capget, &cap_hdr, &cap_data))
    exit(1);
  const int drop = (1 << CAP_SYS_PTRACE) | (1 << CAP_SYS_NICE);
  cap_data[0].effective &= ~drop;
  cap_data[0].permitted &= ~drop;
  cap_data[0].inheritable &= ~drop;
  if (syscall(SYS_capset, &cap_hdr, &cap_data))
    exit(1);
}

static int do_sandbox_none(void)
{
  if (unshare(CLONE_NEWPID)) {
  }
  int pid = fork();
  if (pid != 0)
    return wait_for_loop(pid);
  setup_common();
  sandbox_common();
  drop_caps();
  initialize_netdevices_init();
  if (unshare(CLONE_NEWNET)) {
  }
  initialize_devlink_pci();
  initialize_netdevices();
  loop();
  exit(1);
}

static void kill_and_wait(int pid, int* status)
{
  kill(-pid, SIGKILL);
  kill(pid, SIGKILL);
  int i;
  for (i = 0; i < 100; i++) {
    if (waitpid(-1, status, WNOHANG | __WALL) == pid)
      return;
    usleep(1000);
  }
  DIR* dir = opendir("/sys/fs/fuse/connections");
  if (dir) {
    for (;;) {
      struct dirent* ent = readdir(dir);
      if (!ent)
        break;
      if (strcmp(ent->d_name, ".") == 0 || strcmp(ent->d_name, "..") == 0)
        continue;
      char abort[300];
      snprintf(abort, sizeof(abort), "/sys/fs/fuse/connections/%s/abort",
               ent->d_name);
      int fd = open(abort, O_WRONLY);
      if (fd == -1) {
        continue;
      }
      if (write(fd, abort, 1) < 0) {
      }
      close(fd);
    }
    closedir(dir);
  } else {
  }
  while (waitpid(-1, status, __WALL) != pid) {
  }
}

static void setup_test()
{
  prctl(PR_SET_PDEATHSIG, SIGKILL, 0, 0, 0);
  setpgrp();
  write_file("/proc/self/oom_score_adj", "1000");
}

static void close_fds()
{
  int fd;
  for (fd = 3; fd < MAX_FDS; fd++)
    close(fd);
}

struct thread_t {
  int created, call;
  event_t ready, done;
};

static struct thread_t threads[16];
static void execute_call(int call);
static int running;

static void* thr(void* arg)
{
  struct thread_t* th = (struct thread_t*)arg;
  for (;;) {
    event_wait(&th->ready);
    event_reset(&th->ready);
    execute_call(th->call);
    __atomic_fetch_sub(&running, 1, __ATOMIC_RELAXED);
    event_set(&th->done);
  }
  return 0;
}

static void execute_one(void)
{
  int i, call, thread;
  for (call = 0; call < 56; call++) {
    for (thread = 0; thread < (int)(sizeof(threads) / sizeof(threads[0]));
         thread++) {
      struct thread_t* th = &threads[thread];
      if (!th->created) {
        th->created = 1;
        event_init(&th->ready);
        event_init(&th->done);
        event_set(&th->done);
        thread_start(thr, th);
      }
      if (!event_isset(&th->done))
        continue;
      event_reset(&th->done);
      th->call = call;
      __atomic_fetch_add(&running, 1, __ATOMIC_RELAXED);
      event_set(&th->ready);
      event_timedwait(&th->done, 45);
      break;
    }
  }
  for (i = 0; i < 100 && __atomic_load_n(&running, __ATOMIC_RELAXED); i++)
    sleep_ms(1);
  close_fds();
}

static void execute_one(void);

#define WAIT_FLAGS __WALL

static void loop(void)
{
  int iter;
  for (iter = 0;; iter++) {
    int pid = fork();
    if (pid < 0)
      exit(1);
    if (pid == 0) {
      setup_test();
      execute_one();
      exit(0);
    }
    int status = 0;
    uint64_t start = current_time_ms();
    for (;;) {
      if (waitpid(-1, &status, WNOHANG | WAIT_FLAGS) == pid)
        break;
      sleep_ms(1);
      if (current_time_ms() - start < 5 * 1000)
        continue;
      kill_and_wait(pid, &status);
      break;
    }
  }
}

#ifndef __NR_bpf
#define __NR_bpf 321
#endif
#ifndef __NR_memfd_create
#define __NR_memfd_create 319
#endif

uint64_t r[15] = {0xffffffffffffffff,
                  0x0,
                  0xffffffffffffffff,
                  0xffffffffffffffff,
                  0x0,
                  0xffffffffffffffff,
                  0xffffffffffffffff,
                  0xffffffffffffffff,
                  0xffffffffffffffff,
                  0xffffffffffffffff,
                  0xffffffffffffffff,
                  0xffffffffffffffff,
                  0xffffffffffffffff,
                  0xffffffffffffffff,
                  0x0};

void execute_call(int call)
{
  intptr_t res;
  switch (call) {
  case 0:
    res = syscall(__NR_socket, 2ul, 2ul, 0x88ul);
    if (res != -1)
      r[0] = res;
    break;
  case 1:
    NONFAILING(memcpy((void*)0x20001840,
                      "vcan0\000\000\000\000\000\000\000\000\000\000\000", 16));
    NONFAILING(*(uint32_t*)0x20001850 = 0);
    res = syscall(__NR_ioctl, r[0], 0x8933ul, 0x20001840ul);
    if (res != -1)
      NONFAILING(r[1] = *(uint32_t*)0x20001850);
    break;
  case 2:
    res = syscall(__NR_socket, 0x1dul, 2ul, 7ul);
    if (res != -1)
      r[2] = res;
    break;
  case 3:
    res = syscall(__NR_socket, 2ul, 2ul, 0x88ul);
    if (res != -1)
      r[3] = res;
    break;
  case 4:
    NONFAILING(memcpy((void*)0x20001840,
                      "vcan0\000\000\000\000\000\000\000\000\000\000\000", 16));
    NONFAILING(*(uint32_t*)0x20001850 = 0);
    res = syscall(__NR_ioctl, r[3], 0x8933ul, 0x20001840ul);
    if (res != -1)
      NONFAILING(r[4] = *(uint32_t*)0x20001850);
    break;
  case 5:
    syscall(__NR_socketpair, 1ul, 1ul, 0ul, 0x20000040ul);
    break;
  case 6:
    NONFAILING(*(uint32_t*)0x200001c0 = 0);
    NONFAILING(memcpy((void*)0x200001c4, "\x8a\xd5\x77\xe0\xdc\x47\x17\xf8\x5d"
                                         "\x3c\x39\x2b\xf4\xbb\xa1\xa6\x24\x73"
                                         "\xca\x27\x89\x3a\xa8\xf7\x03\x34\x70"
                                         "\x90\x46\x63\x6e\x0e",
                      32));
    NONFAILING(*(uint32_t*)0x200001e4 = 0);
    NONFAILING(*(uint32_t*)0x200001e8 = 0);
    NONFAILING(*(uint32_t*)0x200001ec = 0);
    NONFAILING(*(uint32_t*)0x200001f0 = 0);
    NONFAILING(*(uint32_t*)0x200001f4 = 0);
    NONFAILING(*(uint32_t*)0x200001f8 = 0);
    NONFAILING(*(uint32_t*)0x200001fc = 0);
    NONFAILING(*(uint32_t*)0x20000200 = 0);
    NONFAILING(*(uint32_t*)0x20000204 = 0);
    NONFAILING(*(uint32_t*)0x20000208 = 0);
    NONFAILING(*(uint32_t*)0x2000020c = 0);
    NONFAILING(*(uint32_t*)0x20000210 = 0);
    syscall(__NR_ioctl, -1, 0xc054561dul, 0x200001c0ul);
    break;
  case 7:
    NONFAILING(memcpy((void*)0x20000000, "/dev/radio#\000", 12));
    res = syz_open_dev(0x20000000, 0, 2);
    if (res != -1)
      r[5] = res;
    break;
  case 8:
    NONFAILING(*(uint32_t*)0x200001c0 = 0);
    NONFAILING(memcpy((void*)0x200001c4, "\x8a\xd5\x77\xe0\xdc\x47\x17\xf8\x5d"
                                         "\x3c\x39\x2b\xf4\xbb\xa1\xa6\x24\x73"
                                         "\xca\x27\x89\x3a\xa8\xf7\x03\x34\x70"
                                         "\x90\x46\x63\x6e\x0e",
                      32));
    NONFAILING(*(uint32_t*)0x200001e4 = 0);
    NONFAILING(*(uint32_t*)0x200001e8 = 0);
    NONFAILING(*(uint32_t*)0x200001ec = 0);
    NONFAILING(*(uint32_t*)0x200001f0 = 0);
    NONFAILING(*(uint32_t*)0x200001f4 = 0);
    NONFAILING(*(uint32_t*)0x200001f8 = 0);
    NONFAILING(*(uint32_t*)0x200001fc = 0);
    NONFAILING(*(uint32_t*)0x20000200 = 0);
    NONFAILING(*(uint32_t*)0x20000204 = 0);
    NONFAILING(*(uint32_t*)0x20000208 = 0);
    NONFAILING(*(uint32_t*)0x2000020c = 0);
    NONFAILING(*(uint32_t*)0x20000210 = 0);
    syscall(__NR_ioctl, r[5], 0xc054561dul, 0x200001c0ul);
    break;
  case 9:
    NONFAILING(memcpy((void*)0x20000000, "/dev/radio#\000", 12));
    res = syz_open_dev(0x20000000, 0, 2);
    if (res != -1)
      r[6] = res;
    break;
  case 10:
    NONFAILING(*(uint32_t*)0x200001c0 = 0);
    NONFAILING(memcpy((void*)0x200001c4, "\x8a\xd5\x77\xe0\xdc\x47\x17\xf8\x5d"
                                         "\x3c\x39\x2b\xf4\xbb\xa1\xa6\x24\x73"
                                         "\xca\x27\x89\x3a\xa8\xf7\x03\x34\x70"
                                         "\x90\x46\x63\x6e\x0e",
                      32));
    NONFAILING(*(uint32_t*)0x200001e4 = 0);
    NONFAILING(*(uint32_t*)0x200001e8 = 0);
    NONFAILING(*(uint32_t*)0x200001ec = 0);
    NONFAILING(*(uint32_t*)0x200001f0 = 0);
    NONFAILING(*(uint32_t*)0x200001f4 = 0);
    NONFAILING(*(uint32_t*)0x200001f8 = 0);
    NONFAILING(*(uint32_t*)0x200001fc = 0);
    NONFAILING(*(uint32_t*)0x20000200 = 0);
    NONFAILING(*(uint32_t*)0x20000204 = 0);
    NONFAILING(*(uint32_t*)0x20000208 = 0);
    NONFAILING(*(uint32_t*)0x2000020c = 0);
    NONFAILING(*(uint32_t*)0x20000210 = 0);
    syscall(__NR_ioctl, r[6], 0xc054561dul, 0x200001c0ul);
    break;
  case 11:
    NONFAILING(memcpy((void*)0x20000000, "/dev/radio#\000", 12));
    syz_open_dev(0x20000000, 0, 2);
    break;
  case 12:
    NONFAILING(memcpy((void*)0x20000000, "/dev/radio#\000", 12));
    res = syz_open_dev(0x20000000, 0, 2);
    if (res != -1)
      r[7] = res;
    break;
  case 13:
    NONFAILING(*(uint32_t*)0x200001c0 = 0);
    NONFAILING(memcpy((void*)0x200001c4, "\x8a\xd5\x77\xe0\xdc\x47\x17\xf8\x5d"
                                         "\x3c\x39\x2b\xf4\xbb\xa1\xa6\x24\x73"
                                         "\xca\x27\x89\x3a\xa8\xf7\x03\x34\x70"
                                         "\x90\x46\x63\x6e\x0e",
                      32));
    NONFAILING(*(uint32_t*)0x200001e4 = 0);
    NONFAILING(*(uint32_t*)0x200001e8 = 0);
    NONFAILING(*(uint32_t*)0x200001ec = 0);
    NONFAILING(*(uint32_t*)0x200001f0 = 0);
    NONFAILING(*(uint32_t*)0x200001f4 = 0);
    NONFAILING(*(uint32_t*)0x200001f8 = 0);
    NONFAILING(*(uint32_t*)0x200001fc = 0);
    NONFAILING(*(uint32_t*)0x20000200 = 0);
    NONFAILING(*(uint32_t*)0x20000204 = 0);
    NONFAILING(*(uint32_t*)0x20000208 = 0);
    NONFAILING(*(uint32_t*)0x2000020c = 0);
    NONFAILING(*(uint32_t*)0x20000210 = 0);
    syscall(__NR_ioctl, r[7], 0xc054561dul, 0x200001c0ul);
    break;
  case 14:
    NONFAILING(memcpy((void*)0x20000000, "/dev/adsp#\000", 11));
    res = syz_open_dev(0x20000000, 1, 1);
    if (res != -1)
      r[8] = res;
    break;
  case 15:
    syz_open_dev(0, 0x80000001, 4);
    break;
  case 16:
    NONFAILING(*(uint16_t*)0x20000240 = 0x1d);
    NONFAILING(*(uint32_t*)0x20000244 = r[4]);
    NONFAILING(*(uint64_t*)0x20000248 = 0);
    NONFAILING(*(uint8_t*)0x20000250 = 0);
    NONFAILING(*(uint8_t*)0x20000251 = 0);
    NONFAILING(*(uint8_t*)0x20000252 = 0);
    NONFAILING(*(uint8_t*)0x20000253 = 0);
    NONFAILING(*(uint8_t*)0x20000254 = 0);
    syscall(__NR_bind, r[2], 0x20000240ul, 0x18ul);
    break;
  case 17:
    syscall(__NR_setsockopt, r[8], 6ul, 0xeul, 0ul, 0ul);
    break;
  case 18:
    NONFAILING(*(uint16_t*)0x20000180 = 0x1d);
    NONFAILING(*(uint32_t*)0x20000184 = r[1]);
    NONFAILING(*(uint64_t*)0x20000188 = 0);
    NONFAILING(*(uint8_t*)0x20000190 = 0);
    NONFAILING(*(uint8_t*)0x20000191 = 0);
    NONFAILING(*(uint8_t*)0x20000192 = 0);
    NONFAILING(*(uint8_t*)0x20000193 = 0);
    NONFAILING(*(uint8_t*)0x20000194 = 0);
    syscall(__NR_connect, r[2], 0x20000180ul, 0x18ul);
    break;
  case 19:
    syscall(__NR_pipe, 0ul);
    break;
  case 20:
    syscall(__NR_vmsplice, -1, 0ul, 0ul, 0ul);
    break;
  case 21:
    syscall(__NR_getpgid, 0);
    break;
  case 22:
    NONFAILING(memcpy((void*)0x20000140, ".\351\000", 3));
    syscall(__NR_memfd_create, 0x20000140ul, 2ul);
    break;
  case 23:
    NONFAILING(memcpy((void*)0x200003c0, "/dev/snd/seq\000", 13));
    syz_open_dev(0x200003c0, 0, 1);
    break;
  case 24:
    NONFAILING(*(uint32_t*)0x2001d000 = 1);
    NONFAILING(*(uint32_t*)0x2001d004 = 0x70);
    NONFAILING(*(uint8_t*)0x2001d008 = 0);
    NONFAILING(*(uint8_t*)0x2001d009 = 0);
    NONFAILING(*(uint8_t*)0x2001d00a = 0);
    NONFAILING(*(uint8_t*)0x2001d00b = 0);
    NONFAILING(*(uint32_t*)0x2001d00c = 0);
    NONFAILING(*(uint64_t*)0x2001d010 = 0x41c1);
    NONFAILING(*(uint64_t*)0x2001d018 = 0);
    NONFAILING(*(uint64_t*)0x2001d020 = 0);
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 0, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 1, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 2, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 3, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 4, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 3, 5, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 6, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 7, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 8, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 9, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 10, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 11, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 12, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 13, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 14, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 15, 2));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 17, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 18, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 19, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 20, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 21, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 22, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 23, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 24, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 25, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 26, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 27, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 28, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 29, 35));
    NONFAILING(*(uint32_t*)0x2001d030 = 0);
    NONFAILING(*(uint32_t*)0x2001d034 = 0);
    NONFAILING(*(uint64_t*)0x2001d038 = 0);
    NONFAILING(*(uint64_t*)0x2001d040 = 0);
    NONFAILING(*(uint64_t*)0x2001d048 = 0);
    NONFAILING(*(uint64_t*)0x2001d050 = 0);
    NONFAILING(*(uint32_t*)0x2001d058 = 0);
    NONFAILING(*(uint32_t*)0x2001d05c = 0);
    NONFAILING(*(uint64_t*)0x2001d060 = 0);
    NONFAILING(*(uint32_t*)0x2001d068 = 0);
    NONFAILING(*(uint16_t*)0x2001d06c = 0);
    NONFAILING(*(uint16_t*)0x2001d06e = 0);
    res = syscall(__NR_perf_event_open, 0x2001d000ul, 0, -1ul, -1, 0ul);
    if (res != -1)
      r[9] = res;
    break;
  case 25:
    res = syscall(__NR_inotify_init1, 0ul);
    if (res != -1)
      r[10] = res;
    break;
  case 26:
    syscall(__NR_fcntl, r[10], 8ul, -1);
    break;
  case 27:
    NONFAILING(memcpy((void*)0x20000240, "system.posix_acl_default\000", 25));
    syscall(__NR_fsetxattr, -1, 0x20000240ul, 0ul, 0ul, 1ul);
    break;
  case 28:
    res = syscall(__NR_socket, 0xaul, 3ul, 8);
    if (res != -1)
      r[11] = res;
    break;
  case 29:
    syscall(__NR_ioctl, r[11], 0x89a2ul, 0ul);
    break;
  case 30:
    syscall(__NR_waitid, 0x83b895581628fca4ul, 0, 0x20000040ul, 2ul, 0ul);
    break;
  case 31:
    res = syscall(__NR_dup2, -1, -1);
    if (res != -1)
      r[12] = res;
    break;
  case 32:
    NONFAILING(*(uint32_t*)0x20000140 = 0);
    NONFAILING(*(uint32_t*)0x20000144 = 0);
    NONFAILING(*(uint32_t*)0x20000148 = 0);
    NONFAILING(memcpy((void*)0x2000014c,
                      "queue1\000\000\000\000\000\000\000\0001;"
                      "\000\000\000\000\000\000\000\000\000\000\000\000\000\000"
                      "\000\000\000\001\000\000\006\000\000\000\000\000\314\277"
                      "}\335\000\000\000\000\000\000\000\000\000\000\000\000"
                      "\000\262\036\000\000\000",
                      64));
    NONFAILING(*(uint32_t*)0x2000018c = 0);
    NONFAILING(*(uint8_t*)0x20000190 = 0);
    NONFAILING(*(uint8_t*)0x20000191 = 0);
    NONFAILING(*(uint8_t*)0x20000192 = 0);
    NONFAILING(*(uint8_t*)0x20000193 = 0);
    NONFAILING(*(uint8_t*)0x20000194 = 0);
    NONFAILING(*(uint8_t*)0x20000195 = 0);
    NONFAILING(*(uint8_t*)0x20000196 = 0);
    NONFAILING(*(uint8_t*)0x20000197 = 0);
    NONFAILING(*(uint8_t*)0x20000198 = 0);
    NONFAILING(*(uint8_t*)0x20000199 = 0);
    NONFAILING(*(uint8_t*)0x2000019a = 0);
    NONFAILING(*(uint8_t*)0x2000019b = 0);
    NONFAILING(*(uint8_t*)0x2000019c = 0);
    NONFAILING(*(uint8_t*)0x2000019d = 0);
    NONFAILING(*(uint8_t*)0x2000019e = 0);
    NONFAILING(*(uint8_t*)0x2000019f = 0);
    NONFAILING(*(uint8_t*)0x200001a0 = 0);
    NONFAILING(*(uint8_t*)0x200001a1 = 0);
    NONFAILING(*(uint8_t*)0x200001a2 = 0);
    NONFAILING(*(uint8_t*)0x200001a3 = 0);
    NONFAILING(*(uint8_t*)0x200001a4 = 0);
    NONFAILING(*(uint8_t*)0x200001a5 = 0);
    NONFAILING(*(uint8_t*)0x200001a6 = 0);
    NONFAILING(*(uint8_t*)0x200001a7 = 0);
    NONFAILING(*(uint8_t*)0x200001a8 = 0);
    NONFAILING(*(uint8_t*)0x200001a9 = 0);
    NONFAILING(*(uint8_t*)0x200001aa = 0);
    NONFAILING(*(uint8_t*)0x200001ab = 0);
    NONFAILING(*(uint8_t*)0x200001ac = 0);
    NONFAILING(*(uint8_t*)0x200001ad = 0);
    NONFAILING(*(uint8_t*)0x200001ae = 0);
    NONFAILING(*(uint8_t*)0x200001af = 0);
    NONFAILING(*(uint8_t*)0x200001b0 = 0);
    NONFAILING(*(uint8_t*)0x200001b1 = 0);
    NONFAILING(*(uint8_t*)0x200001b2 = 0);
    NONFAILING(*(uint8_t*)0x200001b3 = 0);
    NONFAILING(*(uint8_t*)0x200001b4 = 0);
    NONFAILING(*(uint8_t*)0x200001b5 = 0);
    NONFAILING(*(uint8_t*)0x200001b6 = 0);
    NONFAILING(*(uint8_t*)0x200001b7 = 0);
    NONFAILING(*(uint8_t*)0x200001b8 = 0);
    NONFAILING(*(uint8_t*)0x200001b9 = 0);
    NONFAILING(*(uint8_t*)0x200001ba = 0);
    NONFAILING(*(uint8_t*)0x200001bb = 0);
    NONFAILING(*(uint8_t*)0x200001bc = 0);
    NONFAILING(*(uint8_t*)0x200001bd = 0);
    NONFAILING(*(uint8_t*)0x200001be = 0);
    NONFAILING(*(uint8_t*)0x200001bf = 0);
    NONFAILING(*(uint8_t*)0x200001c0 = 0);
    NONFAILING(*(uint8_t*)0x200001c1 = 0);
    NONFAILING(*(uint8_t*)0x200001c2 = 0);
    NONFAILING(*(uint8_t*)0x200001c3 = 0);
    NONFAILING(*(uint8_t*)0x200001c4 = 0);
    NONFAILING(*(uint8_t*)0x200001c5 = 0);
    NONFAILING(*(uint8_t*)0x200001c6 = 0);
    NONFAILING(*(uint8_t*)0x200001c7 = 0);
    NONFAILING(*(uint8_t*)0x200001c8 = 0);
    NONFAILING(*(uint8_t*)0x200001c9 = 0);
    NONFAILING(*(uint8_t*)0x200001ca = 0);
    NONFAILING(*(uint8_t*)0x200001cb = 0);
    syscall(__NR_ioctl, r[12], 0xc08c5332ul, 0x20000140ul);
    break;
  case 33:
    syscall(__NR_prctl, 0x59616d61ul, 0, 0, 0);
    break;
  case 34:
    syz_open_dev(0, 5, 0);
    break;
  case 35:
    NONFAILING(*(uint32_t*)0x20000000 = 0);
    NONFAILING(*(uint32_t*)0x20000004 = 0);
    NONFAILING(*(uint32_t*)0x20000008 = 0);
    syscall(__NR_bpf, 0xdul, 0x20000000ul, 0xcul);
    break;
  case 36:
    NONFAILING(*(uint32_t*)0x200007c0 = -1);
    NONFAILING(*(uint32_t*)0x200007c4 = -1);
    NONFAILING(*(uint32_t*)0x200007c8 = 0);
    NONFAILING(*(uint32_t*)0x200007cc = 0x17);
    NONFAILING(*(uint64_t*)0x200007d0 = 0x20000780);
    NONFAILING(memcpy((void*)0x20000780, "cgroup.subtree_control\000", 23));
    NONFAILING(*(uint32_t*)0x200007d8 = 0);
    NONFAILING(*(uint32_t*)0x200007dc = 0);
    NONFAILING(*(uint64_t*)0x200007e0 = 0);
    NONFAILING(*(uint64_t*)0x200007e8 = 0);
    syscall(__NR_bpf, 0x14ul, 0x200007c0ul, 0x30ul);
    break;
  case 37:
    NONFAILING(*(uint32_t*)0x20000000 = 0);
    NONFAILING(*(uint32_t*)0x20000004 = 0);
    NONFAILING(*(uint32_t*)0x20000008 = 0);
    syscall(__NR_bpf, 0xdul, 0x20000000ul, 0xcul);
    break;
  case 38:
    NONFAILING(*(uint32_t*)0x200001c0 = 0);
    NONFAILING(*(uint32_t*)0x200001c4 = r[9]);
    NONFAILING(*(uint32_t*)0x200001c8 = 0);
    NONFAILING(*(uint32_t*)0x200001cc = 4);
    NONFAILING(*(uint64_t*)0x200001d0 = 0x20000180);
    NONFAILING(memcpy((void*)0x20000180, "em1\000", 4));
    NONFAILING(*(uint32_t*)0x200001d8 = 0);
    NONFAILING(*(uint32_t*)0x200001dc = 0);
    NONFAILING(*(uint64_t*)0x200001e0 = 0);
    NONFAILING(*(uint64_t*)0x200001e8 = 0);
    syscall(__NR_bpf, 0x14ul, 0x200001c0ul, 0x30ul);
    break;
  case 39:
    syscall(__NR_bpf, 0xdul, 0ul, 0ul);
    break;
  case 40:
    syscall(__NR_bpf, 0x14ul, 0ul, 0ul);
    break;
  case 41:
    NONFAILING(memcpy((void*)0x20000000, "/dev/radio#\000", 12));
    res = syz_open_dev(0x20000000, 0, 2);
    if (res != -1)
      r[13] = res;
    break;
  case 42:
    NONFAILING(*(uint32_t*)0x200001c0 = 0);
    NONFAILING(memcpy((void*)0x200001c4, "\x8a\xd5\x77\xe0\xdc\x47\x17\xf8\x5d"
                                         "\x3c\x39\x2b\xf4\xbb\xa1\xa6\x24\x73"
                                         "\xca\x27\x89\x3a\xa8\xf7\x03\x34\x70"
                                         "\x90\x46\x63\x6e\x0e",
                      32));
    NONFAILING(*(uint32_t*)0x200001e4 = 0);
    NONFAILING(*(uint32_t*)0x200001e8 = 0);
    NONFAILING(*(uint32_t*)0x200001ec = 0);
    NONFAILING(*(uint32_t*)0x200001f0 = 0);
    NONFAILING(*(uint32_t*)0x200001f4 = 0);
    NONFAILING(*(uint32_t*)0x200001f8 = 0);
    NONFAILING(*(uint32_t*)0x200001fc = 0);
    NONFAILING(*(uint32_t*)0x20000200 = 0);
    NONFAILING(*(uint32_t*)0x20000204 = 0);
    NONFAILING(*(uint32_t*)0x20000208 = 0);
    NONFAILING(*(uint32_t*)0x2000020c = 0);
    NONFAILING(*(uint32_t*)0x20000210 = 0);
    syscall(__NR_ioctl, r[13], 0xc054561dul, 0x200001c0ul);
    break;
  case 43:
    NONFAILING(*(uint32_t*)0x2001d000 = 1);
    NONFAILING(*(uint32_t*)0x2001d004 = 0x70);
    NONFAILING(*(uint8_t*)0x2001d008 = 0);
    NONFAILING(*(uint8_t*)0x2001d009 = 0);
    NONFAILING(*(uint8_t*)0x2001d00a = 0);
    NONFAILING(*(uint8_t*)0x2001d00b = 0);
    NONFAILING(*(uint32_t*)0x2001d00c = 0);
    NONFAILING(*(uint64_t*)0x2001d010 = 0x41c1);
    NONFAILING(*(uint64_t*)0x2001d018 = 0);
    NONFAILING(*(uint64_t*)0x2001d020 = 0);
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 0, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 1, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 2, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 3, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 4, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 3, 5, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 6, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 7, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 8, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 9, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 10, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 11, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 12, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 13, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 14, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 15, 2));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 17, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 18, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 19, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 20, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 21, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 22, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 23, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 24, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 25, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 26, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 27, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 28, 1));
    NONFAILING(STORE_BY_BITMASK(uint64_t, , 0x2001d028, 0, 29, 35));
    NONFAILING(*(uint32_t*)0x2001d030 = 0);
    NONFAILING(*(uint32_t*)0x2001d034 = 0);
    NONFAILING(*(uint64_t*)0x2001d038 = 0);
    NONFAILING(*(uint64_t*)0x2001d040 = 0);
    NONFAILING(*(uint64_t*)0x2001d048 = 0);
    NONFAILING(*(uint64_t*)0x2001d050 = 0);
    NONFAILING(*(uint32_t*)0x2001d058 = 0);
    NONFAILING(*(uint32_t*)0x2001d05c = 0);
    NONFAILING(*(uint64_t*)0x2001d060 = 0);
    NONFAILING(*(uint32_t*)0x2001d068 = 0);
    NONFAILING(*(uint16_t*)0x2001d06c = 0);
    NONFAILING(*(uint16_t*)0x2001d06e = 0);
    syscall(__NR_perf_event_open, 0x2001d000ul, 0, -1ul, -1, 0ul);
    break;
  case 44:
    NONFAILING(memcpy((void*)0x20000000, "/dev/full\000", 10));
    syscall(__NR_openat, 0xffffffffffffff9cul, 0x20000000ul, 0ul, 0ul);
    break;
  case 45:
    NONFAILING(*(uint32_t*)0x20000080 = 0);
    NONFAILING(*(uint16_t*)0x20000084 = 0x18);
    NONFAILING(*(uint16_t*)0x20000086 = 0xfa00);
    NONFAILING(*(uint64_t*)0x20000088 = 0);
    NONFAILING(*(uint64_t*)0x20000090 = 0x20000040);
    NONFAILING(*(uint16_t*)0x20000098 = 0);
    NONFAILING(*(uint8_t*)0x2000009a = 0);
    NONFAILING(*(uint8_t*)0x2000009b = 0);
    NONFAILING(*(uint8_t*)0x2000009c = 0);
    NONFAILING(*(uint8_t*)0x2000009d = 0);
    NONFAILING(*(uint8_t*)0x2000009e = 0);
    NONFAILING(*(uint8_t*)0x2000009f = 0);
    syscall(__NR_write, -1, 0x20000080ul, 0x20ul);
    break;
  case 46:
    syscall(__NR_ioctl, -1, 0xc08c5332ul, 0ul);
    break;
  case 47:
    res = syscall(__NR_gettid);
    if (res != -1)
      r[14] = res;
    break;
  case 48:
    NONFAILING(memcpy((void*)0x20001580, "/dev/sg#\000", 9));
    syz_open_dev(0x20001580, 0, 5);
    break;
  case 49:
    syscall(__NR_ptrace, 0x4206ul, r[14], 0ul, 0ul);
    break;
  case 50:
    syz_open_dev(0, 5, 0);
    break;
  case 51:
    syscall(__NR_bpf, 0xful, 0ul, 0ul);
    break;
  case 52:
    NONFAILING(*(uint32_t*)0x20000000 = 0);
    NONFAILING(*(uint32_t*)0x20000004 = 0);
    NONFAILING(*(uint32_t*)0x20000008 = 0);
    syscall(__NR_bpf, 0xdul, 0x20000000ul, 0xcul);
    break;
  case 53:
    NONFAILING(*(uint32_t*)0x20000100 = 0);
    NONFAILING(*(uint32_t*)0x20000104 = 0);
    NONFAILING(*(uint32_t*)0x20000108 = 0);
    syscall(__NR_bpf, 0xdul, 0x20000100ul, 0x29dul);
    break;
  case 54:
    syscall(__NR_bpf, 0x14ul, 0ul, 0ul);
    break;
  case 55:
    NONFAILING(*(uint64_t*)0x200004c0 = 0x20000340);
    NONFAILING(memcpy((void*)0x20000340, "\xd2", 1));
    NONFAILING(*(uint64_t*)0x200004c8 = 1);
    syscall(__NR_writev, r[2], 0x200004c0ul, 1ul);
    break;
  }
}
int main(void)
{
  syscall(__NR_mmap, 0x20000000ul, 0x1000000ul, 3ul, 0x32ul, -1, 0);
  install_segv_handler();
  do_sandbox_none();
  return 0;
}
