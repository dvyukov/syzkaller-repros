// WARNING in iomap_iter
// https://syzkaller.appspot.com/bug?id=a8e049cd3abd342936b6
// status:0
// autogenerated by syzkaller (https://github.com/google/syzkaller)

#define _GNU_SOURCE

#include <dirent.h>
#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <signal.h>
#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/prctl.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <time.h>
#include <unistd.h>

static void sleep_ms(uint64_t ms)
{
  usleep(ms * 1000);
}

static uint64_t current_time_ms(void)
{
  struct timespec ts;
  if (clock_gettime(CLOCK_MONOTONIC, &ts))
    exit(1);
  return (uint64_t)ts.tv_sec * 1000 + (uint64_t)ts.tv_nsec / 1000000;
}

static bool write_file(const char* file, const char* what, ...)
{
  char buf[1024];
  va_list args;
  va_start(args, what);
  vsnprintf(buf, sizeof(buf), what, args);
  va_end(args);
  buf[sizeof(buf) - 1] = 0;
  int len = strlen(buf);
  int fd = open(file, O_WRONLY | O_CLOEXEC);
  if (fd == -1)
    return false;
  if (write(fd, buf, len) != len) {
    int err = errno;
    close(fd);
    errno = err;
    return false;
  }
  close(fd);
  return true;
}

static long syz_open_dev(volatile long a0, volatile long a1, volatile long a2)
{
  if (a0 == 0xc || a0 == 0xb) {
    char buf[128];
    sprintf(buf, "/dev/%s/%d:%d", a0 == 0xc ? "char" : "block", (uint8_t)a1,
            (uint8_t)a2);
    return open(buf, O_RDWR, 0);
  } else {
    char buf[1024];
    char* hash;
    strncpy(buf, (char*)a0, sizeof(buf) - 1);
    buf[sizeof(buf) - 1] = 0;
    while ((hash = strchr(buf, '#'))) {
      *hash = '0' + (char)(a1 % 10);
      a1 /= 10;
    }
    return open(buf, a2, 0);
  }
}

static void kill_and_wait(int pid, int* status)
{
  kill(-pid, SIGKILL);
  kill(pid, SIGKILL);
  for (int i = 0; i < 100; i++) {
    if (waitpid(-1, status, WNOHANG | __WALL) == pid)
      return;
    usleep(1000);
  }
  DIR* dir = opendir("/sys/fs/fuse/connections");
  if (dir) {
    for (;;) {
      struct dirent* ent = readdir(dir);
      if (!ent)
        break;
      if (strcmp(ent->d_name, ".") == 0 || strcmp(ent->d_name, "..") == 0)
        continue;
      char abort[300];
      snprintf(abort, sizeof(abort), "/sys/fs/fuse/connections/%s/abort",
               ent->d_name);
      int fd = open(abort, O_WRONLY);
      if (fd == -1) {
        continue;
      }
      if (write(fd, abort, 1) < 0) {
      }
      close(fd);
    }
    closedir(dir);
  } else {
  }
  while (waitpid(-1, status, __WALL) != pid) {
  }
}

static void setup_test()
{
  prctl(PR_SET_PDEATHSIG, SIGKILL, 0, 0, 0);
  setpgrp();
  write_file("/proc/self/oom_score_adj", "1000");
}

static void execute_one(void);

#define WAIT_FLAGS __WALL

static void loop(void)
{
  int iter = 0;
  for (;; iter++) {
    int pid = fork();
    if (pid < 0)
      exit(1);
    if (pid == 0) {
      setup_test();
      execute_one();
      exit(0);
    }
    int status = 0;
    uint64_t start = current_time_ms();
    for (;;) {
      if (waitpid(-1, &status, WNOHANG | WAIT_FLAGS) == pid)
        break;
      sleep_ms(1);
      if (current_time_ms() - start < 5000)
        continue;
      kill_and_wait(pid, &status);
      break;
    }
  }
}

uint64_t r[3] = {0xffffffffffffffff, 0xffffffffffffffff, 0xffffffffffffffff};

void execute_one(void)
{
  intptr_t res = 0;
  memcpy((void*)0x200001c0, "/dev/loop#\000", 11);
  res = -1;
  res = syz_open_dev(0x200001c0, 0, 0);
  if (res != -1)
    r[0] = res;
  syscall(__NR_ioctl, r[0], 0x4c08, 0xffful);
  memcpy((void*)0x20000000, "./bus\000", 6);
  res = syscall(__NR_open, 0x20000000ul, 0x141042ul, 0ul);
  if (res != -1)
    r[1] = res;
  syscall(__NR_write, r[1], 0x20000200ul, 0x116ul);
  *(uint64_t*)0x20000240 = 1;
  syscall(__NR_sendfile, r[1], r[1], 0x20000240ul, 0x7ffful);
  syscall(__NR_ioctl, r[0], 0x4c00, r[1]);
  memcpy((void*)0x200001c0, "/dev/loop#\000", 11);
  res = -1;
  res = syz_open_dev(0x200001c0, 0, 0);
  if (res != -1)
    r[2] = res;
  *(uint32_t*)0x20000000 = 0;
  *(uint16_t*)0x20000008 = 0;
  *(uint64_t*)0x20000010 = 0;
  *(uint16_t*)0x20000018 = 0;
  *(uint32_t*)0x20000020 = 0x80000000;
  *(uint32_t*)0x20000024 = 0;
  *(uint32_t*)0x20000028 = 0xa;
  *(uint32_t*)0x2000002c = 0x14;
  memcpy((void*)0x20000030,
         "\x75\xdb\xe6\xf4\xd0\x3a\x66\x3b\x26\x8d\x2d\x84\x8e\x04\x54\x5a\x75"
         "\x84\xa3\xa2\xaf\x09\x6f\x2d\x06\xc4\x63\xcf\x4e\x89\x10\x7e\xa8\xf9"
         "\xc0\x07\x7c\x0c\x4b\x04\x79\x52\xba\x72\xb2\x86\xf1\x73\xc0\x5c\x92"
         "\x48\x5d\xcd\x07\xd9\xc1\xb5\xce\x53\xf8\xcd\xbf\x8e",
         64);
  memcpy((void*)0x20000070,
         "\x40\xec\xeb\xec\xe3\x9c\x63\x6f\x13\xc4\x66\x95\xd0\x15\x95\xe0\x0c"
         "\x6e\x91\x9c\x6c\xf3\xef\x6f\x22\xce\x67\x28\xf1\x8b\x49\xa7",
         32);
  *(uint64_t*)0x20000090 = 0x3e;
  *(uint64_t*)0x20000098 = 2;
  *(uint32_t*)0x200000a0 = 0;
  syscall(__NR_ioctl, r[2], 0x4c02, 0x20000000ul);
}
int main(void)
{
  syscall(__NR_mmap, 0x1ffff000ul, 0x1000ul, 0ul, 0x32ul, -1, 0ul);
  syscall(__NR_mmap, 0x20000000ul, 0x1000000ul, 7ul, 0x32ul, -1, 0ul);
  syscall(__NR_mmap, 0x21000000ul, 0x1000ul, 0ul, 0x32ul, -1, 0ul);
  loop();
  return 0;
}
