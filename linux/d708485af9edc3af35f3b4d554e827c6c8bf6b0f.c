// KASAN: slab-out-of-bounds Read in hci_event_packet
// https://syzkaller.appspot.com/bug?id=d708485af9edc3af35f3b4d554e827c6c8bf6b0f
// status:open
// autogenerated by syzkaller (https://github.com/google/syzkaller)

#define _GNU_SOURCE

#include <dirent.h>
#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <signal.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/prctl.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <time.h>
#include <unistd.h>

static void sleep_ms(uint64_t ms)
{
  usleep(ms * 1000);
}

static uint64_t current_time_ms(void)
{
  struct timespec ts;
  if (clock_gettime(CLOCK_MONOTONIC, &ts))
    exit(1);
  return (uint64_t)ts.tv_sec * 1000 + (uint64_t)ts.tv_nsec / 1000000;
}

static void kill_and_wait(int pid, int* status)
{
  kill(-pid, SIGKILL);
  kill(pid, SIGKILL);
  int i;
  for (i = 0; i < 100; i++) {
    if (waitpid(-1, status, WNOHANG | __WALL) == pid)
      return;
    usleep(1000);
  }
  DIR* dir = opendir("/sys/fs/fuse/connections");
  if (dir) {
    for (;;) {
      struct dirent* ent = readdir(dir);
      if (!ent)
        break;
      if (strcmp(ent->d_name, ".") == 0 || strcmp(ent->d_name, "..") == 0)
        continue;
      char abort[300];
      snprintf(abort, sizeof(abort), "/sys/fs/fuse/connections/%s/abort",
               ent->d_name);
      int fd = open(abort, O_WRONLY);
      if (fd == -1) {
        continue;
      }
      if (write(fd, abort, 1) < 0) {
      }
      close(fd);
    }
    closedir(dir);
  } else {
  }
  while (waitpid(-1, status, __WALL) != pid) {
  }
}

#define SYZ_HAVE_SETUP_TEST 1
static void setup_test()
{
  prctl(PR_SET_PDEATHSIG, SIGKILL, 0, 0, 0);
  setpgrp();
}

#define SYZ_HAVE_RESET_TEST 1
static void reset_test()
{
  int fd;
  for (fd = 3; fd < 30; fd++)
    close(fd);
}

static void execute_one(void);

#define WAIT_FLAGS __WALL

static void loop(void)
{
  int iter;
  for (iter = 0;; iter++) {
    int pid = fork();
    if (pid < 0)
      exit(1);
    if (pid == 0) {
      setup_test();
      execute_one();
      reset_test();
      exit(0);
    }
    int status = 0;
    uint64_t start = current_time_ms();
    for (;;) {
      if (waitpid(-1, &status, WNOHANG | WAIT_FLAGS) == pid)
        break;
      sleep_ms(1);
      if (current_time_ms() - start < 5 * 1000)
        continue;
      kill_and_wait(pid, &status);
      break;
    }
  }
}

uint64_t r[1] = {0xffffffffffffffff};

void execute_one(void)
{
  long res = 0;
  memcpy((void*)0x200000c0, "/dev/vhci\x00", 10);
  res = syscall(__NR_openat, 0xffffffffffffff9c, 0x200000c0, 0x8401, 0);
  if (res != -1)
    r[0] = res;
  *(uint8_t*)0x20000140 = -1;
  *(uint8_t*)0x20000141 = 0;
  syscall(__NR_write, r[0], 0x20000140, 2);
  *(uint8_t*)0x200004c0 = 4;
  memcpy((void*)0x200004c1,
         "\x22\xc0\xee\xb1\xed\xb9\x61\xb1\x25\x71\x68\xe1\x3e\x60\xc3\xda\xaa"
         "\x2c\x55\xa2\x23\x0f\xcd\x70\x38\x34\x3c\x40\xe8\x5d\x0d\x25\x5a\xb6"
         "\xe7\x95\xec\xf4\xbe\xcc\xc0\x33\x8d\xed\x39\xc9\xf4\xd8\x9e\xb5\xa5"
         "\xbb\xb5\x5a\xad\xc5\x2e\x8c\xd6\x0f\x2d\x56\xd7\xd4\xf2\xfe\x64\x1c"
         "\x75\xce\xf2\x59\xf1\xed\x90\xf2\x94\x19\x1e\x44\x10\xa8\x56\x75\x8f"
         "\x8f\x54\xce\xc0\xcc\xfd\xa8\xbe\xf9\xdb\x1f\x9e\x34\x38\xc5\x6e\x68"
         "\x41\x6d\x26\x20\xd0\xa2\x4e\xa8\x5d\x2e\x3a\x85\x04\xf0\xe9\xc2\x04"
         "\xf9\xa1\x2b\xd7\x09\x7b\x1a\x6d\x0a\x52\x09\xc3\x0c\xc4\x44\x8b\x9c"
         "\xe1\x6e\xff\x5d\x80\x50\x03\xce\xfe\xa3\xa1\x9d\xf5\x58\x62\x13\x9f"
         "\x90\xfd\xb2\x0d\xc9\xb6\xe5\x5a\x0c\x7b\xea\xb0\x07\xde\x99\x04\x8a"
         "\xd0\x68\x3b\x3c\xb5\x95\xa7\x09\x13\x7e\x0a\x92\xdd\x64\x68\xfc",
         186);
  syscall(__NR_write, r[0], 0x200004c0, 0xbb);
}
int main(void)
{
  syscall(__NR_mmap, 0x20000000, 0x1000000, 3, 0x32, -1, 0);
  loop();
  return 0;
}
