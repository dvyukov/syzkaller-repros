// BUG: "hc->tx_t_ipi == NUM" holds (exception!) at net/dccp/ccids/ccid3.c:LINE/ccid3_update_send_interval()
// https://syzkaller.appspot.com/bug?id=5e6457a20b717fe66d68
// status:0
// autogenerated by syzkaller (https://github.com/google/syzkaller)

#define _GNU_SOURCE

#include <dirent.h>
#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <signal.h>
#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/prctl.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <time.h>
#include <unistd.h>

static void sleep_ms(uint64_t ms)
{
  usleep(ms * 1000);
}

static uint64_t current_time_ms(void)
{
  struct timespec ts;
  if (clock_gettime(CLOCK_MONOTONIC, &ts))
    exit(1);
  return (uint64_t)ts.tv_sec * 1000 + (uint64_t)ts.tv_nsec / 1000000;
}

static bool write_file(const char* file, const char* what, ...)
{
  char buf[1024];
  va_list args;
  va_start(args, what);
  vsnprintf(buf, sizeof(buf), what, args);
  va_end(args);
  buf[sizeof(buf) - 1] = 0;
  int len = strlen(buf);
  int fd = open(file, O_WRONLY | O_CLOEXEC);
  if (fd == -1)
    return false;
  if (write(fd, buf, len) != len) {
    int err = errno;
    close(fd);
    errno = err;
    return false;
  }
  close(fd);
  return true;
}

static void kill_and_wait(int pid, int* status)
{
  kill(-pid, SIGKILL);
  kill(pid, SIGKILL);
  for (int i = 0; i < 100; i++) {
    if (waitpid(-1, status, WNOHANG | __WALL) == pid)
      return;
    usleep(1000);
  }
  DIR* dir = opendir("/sys/fs/fuse/connections");
  if (dir) {
    for (;;) {
      struct dirent* ent = readdir(dir);
      if (!ent)
        break;
      if (strcmp(ent->d_name, ".") == 0 || strcmp(ent->d_name, "..") == 0)
        continue;
      char abort[300];
      snprintf(abort, sizeof(abort), "/sys/fs/fuse/connections/%s/abort",
               ent->d_name);
      int fd = open(abort, O_WRONLY);
      if (fd == -1) {
        continue;
      }
      if (write(fd, abort, 1) < 0) {
      }
      close(fd);
    }
    closedir(dir);
  } else {
  }
  while (waitpid(-1, status, __WALL) != pid) {
  }
}

static void setup_test()
{
  prctl(PR_SET_PDEATHSIG, SIGKILL, 0, 0, 0);
  setpgrp();
  write_file("/proc/self/oom_score_adj", "1000");
}

static void execute_one(void);

#define WAIT_FLAGS __WALL

static void loop(void)
{
  int iter = 0;
  for (;; iter++) {
    int pid = fork();
    if (pid < 0)
      exit(1);
    if (pid == 0) {
      setup_test();
      execute_one();
      exit(0);
    }
    int status = 0;
    uint64_t start = current_time_ms();
    for (;;) {
      if (waitpid(-1, &status, WNOHANG | WAIT_FLAGS) == pid)
        break;
      sleep_ms(1);
      if (current_time_ms() - start < 5000)
        continue;
      kill_and_wait(pid, &status);
      break;
    }
  }
}

uint64_t r[2] = {0xffffffffffffffff, 0xffffffffffffffff};

void execute_one(void)
{
  intptr_t res = 0;
  res = syscall(__NR_socket, 0xaul, 6ul, 0);
  if (res != -1)
    r[0] = res;
  *(uint16_t*)0x20000040 = 0xa;
  *(uint16_t*)0x20000042 = htobe16(0x4e20);
  *(uint32_t*)0x20000044 = htobe32(0);
  memset((void*)0x20000048, 0, 16);
  *(uint32_t*)0x20000058 = 0;
  syscall(__NR_bind, r[0], 0x20000040ul, 0x1cul);
  res = syscall(__NR_socket, 2ul, 6ul, 0);
  if (res != -1)
    r[1] = res;
  syscall(__NR_listen, r[0], 6);
  memset((void*)0x201c9fff, 3, 1);
  syscall(__NR_setsockopt, r[1], 0x10d, 0xd, 0x201c9ffful, 1ul);
  *(uint16_t*)0x20e5c000 = 2;
  *(uint16_t*)0x20e5c002 = htobe16(0x4e20);
  *(uint32_t*)0x20e5c004 = htobe32(0);
  syscall(__NR_connect, r[1], 0x20e5c000ul, 0x10ul);
  memcpy((void*)0x20000080,
         "\x40\xc8\xb8\xf9\x19\x41\x59\xe7\x3c\x44\x09\x3c\x29\x1a", 14);
  syscall(__NR_sendto, r[1], 0x20000080ul, 0xeul, 0ul, 0ul, 0ul);
  {
    int i;
    for (i = 0; i < 64; i++) {
      syscall(__NR_sendto, r[1], 0x20000080ul, 0xeul, 0ul, 0ul, 0ul);
    }
  }
  *(uint64_t*)0x20000fc0 = 0;
  *(uint32_t*)0x20000fc8 = 0;
  *(uint64_t*)0x20000fd0 = 0x200005c0;
  *(uint64_t*)0x200005c0 = 0x200002c0;
  memset((void*)0x200002c0, 81, 1);
  *(uint64_t*)0x200005c8 = 1;
  *(uint64_t*)0x20000fd8 = 1;
  *(uint64_t*)0x20000fe0 = 0;
  *(uint64_t*)0x20000fe8 = 0;
  *(uint32_t*)0x20000ff0 = 0;
  *(uint32_t*)0x20000ff8 = 0;
  syscall(__NR_sendmmsg, r[1], 0x20000fc0ul, 1ul, 0ul);
}
int main(void)
{
  syscall(__NR_mmap, 0x1ffff000ul, 0x1000ul, 0ul, 0x32ul, -1, 0ul);
  syscall(__NR_mmap, 0x20000000ul, 0x1000000ul, 7ul, 0x32ul, -1, 0ul);
  syscall(__NR_mmap, 0x21000000ul, 0x1000ul, 0ul, 0x32ul, -1, 0ul);
  loop();
  return 0;
}
