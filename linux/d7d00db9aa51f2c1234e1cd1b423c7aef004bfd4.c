// INFO: trying to register non-static key in down
// https://syzkaller.appspot.com/bug?id=d7d00db9aa51f2c1234e1cd1b423c7aef004bfd4
// status:invalid
// autogenerated by syzkaller (https://github.com/google/syzkaller)

#define _GNU_SOURCE

#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <stdarg.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mount.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <unistd.h>

#include <linux/usb/ch9.h>

unsigned long long procid;

static void sleep_ms(uint64_t ms)
{
  usleep(ms * 1000);
}

#define USB_MAX_EP_NUM 32

struct usb_device_index {
  struct usb_device_descriptor* dev;
  struct usb_config_descriptor* config;
  unsigned config_length;
  struct usb_interface_descriptor* iface;
  struct usb_endpoint_descriptor* eps[USB_MAX_EP_NUM];
  unsigned eps_num;
};

static bool parse_usb_descriptor(char* buffer, size_t length,
                                 struct usb_device_index* index)
{
  if (length <
      sizeof(*index->dev) + sizeof(*index->config) + sizeof(*index->iface))
    return false;
  index->dev = (struct usb_device_descriptor*)buffer;
  index->config = (struct usb_config_descriptor*)(buffer + sizeof(*index->dev));
  index->config_length = length - sizeof(*index->dev);
  index->iface =
      (struct usb_interface_descriptor*)(buffer + sizeof(*index->dev) +
                                         sizeof(*index->config));
  index->eps_num = 0;
  size_t offset = 0;
  while (true) {
    if (offset == length)
      break;
    if (offset + 1 < length)
      break;
    uint8_t length = buffer[offset];
    uint8_t type = buffer[offset + 1];
    if (type == USB_DT_ENDPOINT) {
      index->eps[index->eps_num] =
          (struct usb_endpoint_descriptor*)(buffer + offset);
      index->eps_num++;
    }
    if (index->eps_num == USB_MAX_EP_NUM)
      break;
    offset += length;
  }
  return true;
}

enum usb_fuzzer_event_type {
  USB_FUZZER_EVENT_INVALID,
  USB_FUZZER_EVENT_CONNECT,
  USB_FUZZER_EVENT_DISCONNECT,
  USB_FUZZER_EVENT_SUSPEND,
  USB_FUZZER_EVENT_RESUME,
  USB_FUZZER_EVENT_CONTROL,
};

struct usb_fuzzer_event {
  uint32_t type;
  uint32_t length;
  char data[0];
};

struct usb_fuzzer_init {
  uint64_t speed;
  const char* driver_name;
  const char* device_name;
};

struct usb_fuzzer_ep_io {
  uint16_t ep;
  uint16_t flags;
  uint32_t length;
  char data[0];
};

#define USB_FUZZER_IOCTL_INIT _IOW('U', 0, struct usb_fuzzer_init)
#define USB_FUZZER_IOCTL_RUN _IO('U', 1)
#define USB_FUZZER_IOCTL_EP0_READ _IOWR('U', 2, struct usb_fuzzer_event)
#define USB_FUZZER_IOCTL_EP0_WRITE _IOW('U', 3, struct usb_fuzzer_ep_io)
#define USB_FUZZER_IOCTL_EP_ENABLE _IOW('U', 4, struct usb_endpoint_descriptor)
#define USB_FUZZER_IOCTL_EP_WRITE _IOW('U', 6, struct usb_fuzzer_ep_io)
#define USB_FUZZER_IOCTL_CONFIGURE _IO('U', 8)
#define USB_FUZZER_IOCTL_VBUS_DRAW _IOW('U', 9, uint32_t)

int usb_fuzzer_open()
{
  return open("/sys/kernel/debug/usb-fuzzer", O_RDWR);
}

int usb_fuzzer_init(int fd, uint32_t speed, const char* driver,
                    const char* device)
{
  struct usb_fuzzer_init arg;
  arg.speed = speed;
  arg.driver_name = driver;
  arg.device_name = device;
  return ioctl(fd, USB_FUZZER_IOCTL_INIT, &arg);
}

int usb_fuzzer_run(int fd)
{
  return ioctl(fd, USB_FUZZER_IOCTL_RUN, 0);
}

int usb_fuzzer_ep0_read(int fd, struct usb_fuzzer_event* event)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EP0_READ, event);
}

int usb_fuzzer_ep0_write(int fd, struct usb_fuzzer_ep_io* io)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EP0_WRITE, io);
}

int usb_fuzzer_ep_write(int fd, struct usb_fuzzer_ep_io* io)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EP_WRITE, io);
}

int usb_fuzzer_ep_enable(int fd, struct usb_endpoint_descriptor* desc)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EP_ENABLE, desc);
}

int usb_fuzzer_configure(int fd)
{
  return ioctl(fd, USB_FUZZER_IOCTL_CONFIGURE, 0);
}

int usb_fuzzer_vbus_draw(int fd, uint32_t power)
{
  return ioctl(fd, USB_FUZZER_IOCTL_VBUS_DRAW, power);
}

#define USB_MAX_PACKET_SIZE 1024

struct usb_fuzzer_control_event {
  struct usb_fuzzer_event inner;
  struct usb_ctrlrequest ctrl;
};

struct usb_fuzzer_ep_io_data {
  struct usb_fuzzer_ep_io inner;
  char data[USB_MAX_PACKET_SIZE];
};

struct vusb_connect_string_descriptor {
  uint32_t len;
  char* str;
} __attribute__((packed));

struct vusb_connect_descriptors {
  uint32_t qual_len;
  char* qual;
  uint32_t bos_len;
  char* bos;
  uint32_t strs_len;
  struct vusb_connect_string_descriptor strs[0];
} __attribute__((packed));

static volatile long syz_usb_connect(volatile long a0, volatile long a1,
                                     volatile long a2, volatile long a3)
{
  int64_t speed = a0;
  int64_t dev_len = a1;
  char* dev = (char*)a2;
  struct vusb_connect_descriptors* conn_descs =
      (struct vusb_connect_descriptors*)a3;
  if (!dev)
    return -1;
  struct usb_device_index index;
  memset(&index, 0, sizeof(index));
  int rv = parse_usb_descriptor(dev, dev_len, &index);
  if (!rv)
    return -1;
  int fd = usb_fuzzer_open();
  if (fd < 0)
    return -1;
  char device[32];
  sprintf(&device[0], "dummy_udc.%llu", procid);
  rv = usb_fuzzer_init(fd, speed, "dummy_udc", &device[0]);
  if (rv < 0)
    return -1;
  rv = usb_fuzzer_run(fd);
  if (rv < 0)
    return -1;
  bool done = false;
  while (!done) {
    char* response_data = NULL;
    uint32_t response_length = 0;
    unsigned ep;
    uint8_t str_idx;
    struct usb_fuzzer_control_event event;
    event.inner.type = 0;
    event.inner.length = sizeof(event.ctrl);
    rv = usb_fuzzer_ep0_read(fd, (struct usb_fuzzer_event*)&event);
    if (rv < 0)
      return -1;
    if (event.inner.type != USB_FUZZER_EVENT_CONTROL)
      continue;
    switch (event.ctrl.bRequestType & USB_TYPE_MASK) {
    case USB_TYPE_STANDARD:
      switch (event.ctrl.bRequest) {
      case USB_REQ_GET_DESCRIPTOR:
        switch (event.ctrl.wValue >> 8) {
        case USB_DT_DEVICE:
          response_data = (char*)index.dev;
          response_length = sizeof(*index.dev);
          goto reply;
        case USB_DT_CONFIG:
          response_data = (char*)index.config;
          response_length = index.config_length;
          goto reply;
        case USB_DT_STRING:
          str_idx = (uint8_t)event.ctrl.wValue;
          if (str_idx >= conn_descs->strs_len)
            goto reply;
          response_data = conn_descs->strs[str_idx].str;
          response_length = conn_descs->strs[str_idx].len;
          goto reply;
        case USB_DT_BOS:
          response_data = conn_descs->bos;
          response_length = conn_descs->bos_len;
          goto reply;
        case USB_DT_DEVICE_QUALIFIER:
          response_data = conn_descs->qual;
          response_length = conn_descs->qual_len;
          goto reply;
        default:
          exit(1);
          continue;
        }
        break;
      case USB_REQ_SET_CONFIGURATION:
        rv = usb_fuzzer_vbus_draw(fd, index.config->bMaxPower);
        if (rv < 0)
          return -1;
        rv = usb_fuzzer_configure(fd);
        if (rv < 0)
          return -1;
        for (ep = 0; ep < index.eps_num; ep++) {
          rv = usb_fuzzer_ep_enable(fd, index.eps[ep]);
          if (rv < 0)
            exit(1);
        }
        done = true;
        goto reply;
      default:
        exit(1);
        continue;
      }
      break;
    default:
      exit(1);
      continue;
    }
    struct usb_fuzzer_ep_io_data response;
  reply:
    response.inner.ep = 0;
    response.inner.flags = 0;
    if (response_length > sizeof(response.data))
      response_length = 0;
    response.inner.length = response_length;
    if (response_data)
      memcpy(&response.data[0], response_data, response_length);
    if (event.ctrl.wLength < response.inner.length)
      response.inner.length = event.ctrl.wLength;
    usb_fuzzer_ep0_write(fd, (struct usb_fuzzer_ep_io*)&response);
  }
  sleep_ms(200);
  return fd;
}

int main(void)
{
  syscall(__NR_mmap, 0x20000000, 0x1000000, 3, 0x32, -1, 0);

  *(uint8_t*)0x20001340 = 0x12;
  *(uint8_t*)0x20001341 = 1;
  *(uint16_t*)0x20001342 = 0;
  *(uint8_t*)0x20001344 = 0x14;
  *(uint8_t*)0x20001345 = 0xd4;
  *(uint8_t*)0x20001346 = 0x21;
  *(uint8_t*)0x20001347 = 8;
  *(uint16_t*)0x20001348 = 0x10cf;
  *(uint16_t*)0x2000134a = 0x5500;
  *(uint16_t*)0x2000134c = 0xe61b;
  *(uint8_t*)0x2000134e = 0;
  *(uint8_t*)0x2000134f = 0;
  *(uint8_t*)0x20001350 = 0;
  *(uint8_t*)0x20001351 = 1;
  *(uint8_t*)0x20001352 = 9;
  *(uint8_t*)0x20001353 = 2;
  *(uint16_t*)0x20001354 = 0x6f;
  *(uint8_t*)0x20001356 = 1;
  *(uint8_t*)0x20001357 = 0;
  *(uint8_t*)0x20001358 = 0;
  *(uint8_t*)0x20001359 = 0;
  *(uint8_t*)0x2000135a = 0;
  *(uint8_t*)0x2000135b = 9;
  *(uint8_t*)0x2000135c = 4;
  *(uint8_t*)0x2000135d = 0x79;
  *(uint8_t*)0x2000135e = 0;
  *(uint8_t*)0x2000135f = 9;
  *(uint8_t*)0x20001360 = 0xae;
  *(uint8_t*)0x20001361 = 0x33;
  *(uint8_t*)0x20001362 = 0x3d;
  *(uint8_t*)0x20001363 = 0;
  *(uint8_t*)0x20001364 = 7;
  *(uint8_t*)0x20001365 = 5;
  *(uint8_t*)0x20001366 = 7;
  *(uint8_t*)0x20001367 = 0x10;
  *(uint16_t*)0x20001368 = 0x29;
  *(uint8_t*)0x2000136a = 6;
  *(uint8_t*)0x2000136b = 6;
  *(uint8_t*)0x2000136c = 3;
  *(uint8_t*)0x2000136d = 7;
  *(uint8_t*)0x2000136e = 5;
  *(uint8_t*)0x2000136f = 0x3f;
  *(uint8_t*)0x20001370 = 8;
  *(uint16_t*)0x20001371 = 4;
  *(uint8_t*)0x20001373 = 9;
  *(uint8_t*)0x20001374 = 8;
  *(uint8_t*)0x20001375 = 2;
  *(uint8_t*)0x20001376 = 7;
  *(uint8_t*)0x20001377 = 5;
  *(uint8_t*)0x20001378 = 8;
  *(uint8_t*)0x20001379 = 4;
  *(uint16_t*)0x2000137a = 0x8001;
  *(uint8_t*)0x2000137c = 1;
  *(uint8_t*)0x2000137d = 0;
  *(uint8_t*)0x2000137e = 3;
  *(uint8_t*)0x2000137f = 7;
  *(uint8_t*)0x20001380 = 5;
  *(uint8_t*)0x20001381 = 0;
  *(uint8_t*)0x20001382 = 2;
  *(uint16_t*)0x20001383 = 1;
  *(uint8_t*)0x20001385 = 4;
  *(uint8_t*)0x20001386 = 5;
  *(uint8_t*)0x20001387 = 9;
  *(uint8_t*)0x20001388 = 7;
  *(uint8_t*)0x20001389 = 5;
  *(uint8_t*)0x2000138a = 1;
  *(uint8_t*)0x2000138b = 0;
  *(uint16_t*)0x2000138c = 0x100;
  *(uint8_t*)0x2000138e = 3;
  *(uint8_t*)0x2000138f = 0xb6;
  *(uint8_t*)0x20001390 = 9;
  *(uint8_t*)0x20001391 = 7;
  *(uint8_t*)0x20001392 = 5;
  *(uint8_t*)0x20001393 = 0;
  *(uint8_t*)0x20001394 = 3;
  *(uint16_t*)0x20001395 = 0;
  *(uint8_t*)0x20001397 = 7;
  *(uint8_t*)0x20001398 = 0xfa;
  *(uint8_t*)0x20001399 = 0x80;
  *(uint8_t*)0x2000139a = 2;
  *(uint8_t*)0x2000139b = 0xe;
  *(uint8_t*)0x2000139c = 7;
  *(uint8_t*)0x2000139d = 5;
  *(uint8_t*)0x2000139e = 0x8f;
  *(uint8_t*)0x2000139f = 1;
  *(uint16_t*)0x200013a0 = 6;
  *(uint8_t*)0x200013a2 = -1;
  *(uint8_t*)0x200013a3 = 6;
  *(uint8_t*)0x200013a4 = 0x62;
  *(uint8_t*)0x200013a5 = 2;
  *(uint8_t*)0x200013a6 = 0x22;
  *(uint8_t*)0x200013a7 = 7;
  *(uint8_t*)0x200013a8 = 5;
  *(uint8_t*)0x200013a9 = 5;
  *(uint8_t*)0x200013aa = 0;
  *(uint16_t*)0x200013ab = 2;
  *(uint8_t*)0x200013ad = 0x7c;
  *(uint8_t*)0x200013ae = 0;
  *(uint8_t*)0x200013af = 1;
  *(uint8_t*)0x200013b0 = 2;
  *(uint8_t*)0x200013b1 = 0x33;
  *(uint8_t*)0x200013b2 = 2;
  *(uint8_t*)0x200013b3 = 7;
  *(uint8_t*)0x200013b4 = 7;
  *(uint8_t*)0x200013b5 = 5;
  *(uint8_t*)0x200013b6 = 0x65;
  *(uint8_t*)0x200013b7 = 0;
  *(uint16_t*)0x200013b8 = 4;
  *(uint8_t*)0x200013ba = 9;
  *(uint8_t*)0x200013bb = 7;
  *(uint8_t*)0x200013bc = 0;
  *(uint8_t*)0x200013bd = 2;
  *(uint8_t*)0x200013be = 0xf;
  *(uint8_t*)0x200013bf = 2;
  *(uint8_t*)0x200013c0 = 0x3f;
  syz_usb_connect(4, 0x81, 0x20001340, 0);
  return 0;
}
