// KMSAN: uninit-value in aes_encrypt (4)
// https://syzkaller.appspot.com/bug?id=828dfc12440b4f6f305d
// status:0
// autogenerated by syzkaller (https://github.com/google/syzkaller)

#define _GNU_SOURCE

#include <dirent.h>
#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <pthread.h>
#include <signal.h>
#include <stdarg.h>
#include <stdbool.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/prctl.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <time.h>
#include <unistd.h>

#include <linux/futex.h>

static void sleep_ms(uint64_t ms)
{
  usleep(ms * 1000);
}

static uint64_t current_time_ms(void)
{
  struct timespec ts;
  if (clock_gettime(CLOCK_MONOTONIC, &ts))
    exit(1);
  return (uint64_t)ts.tv_sec * 1000 + (uint64_t)ts.tv_nsec / 1000000;
}

static void thread_start(void* (*fn)(void*), void* arg)
{
  pthread_t th;
  pthread_attr_t attr;
  pthread_attr_init(&attr);
  pthread_attr_setstacksize(&attr, 128 << 10);
  int i = 0;
  for (; i < 100; i++) {
    if (pthread_create(&th, &attr, fn, arg) == 0) {
      pthread_attr_destroy(&attr);
      return;
    }
    if (errno == EAGAIN) {
      usleep(50);
      continue;
    }
    break;
  }
  exit(1);
}

typedef struct {
  int state;
} event_t;

static void event_init(event_t* ev)
{
  ev->state = 0;
}

static void event_reset(event_t* ev)
{
  ev->state = 0;
}

static void event_set(event_t* ev)
{
  if (ev->state)
    exit(1);
  __atomic_store_n(&ev->state, 1, __ATOMIC_RELEASE);
  syscall(SYS_futex, &ev->state, FUTEX_WAKE | FUTEX_PRIVATE_FLAG, 1000000);
}

static void event_wait(event_t* ev)
{
  while (!__atomic_load_n(&ev->state, __ATOMIC_ACQUIRE))
    syscall(SYS_futex, &ev->state, FUTEX_WAIT | FUTEX_PRIVATE_FLAG, 0, 0);
}

static int event_isset(event_t* ev)
{
  return __atomic_load_n(&ev->state, __ATOMIC_ACQUIRE);
}

static int event_timedwait(event_t* ev, uint64_t timeout)
{
  uint64_t start = current_time_ms();
  uint64_t now = start;
  for (;;) {
    uint64_t remain = timeout - (now - start);
    struct timespec ts;
    ts.tv_sec = remain / 1000;
    ts.tv_nsec = (remain % 1000) * 1000 * 1000;
    syscall(SYS_futex, &ev->state, FUTEX_WAIT | FUTEX_PRIVATE_FLAG, 0, &ts);
    if (__atomic_load_n(&ev->state, __ATOMIC_ACQUIRE))
      return 1;
    now = current_time_ms();
    if (now - start > timeout)
      return 0;
  }
}

static bool write_file(const char* file, const char* what, ...)
{
  char buf[1024];
  va_list args;
  va_start(args, what);
  vsnprintf(buf, sizeof(buf), what, args);
  va_end(args);
  buf[sizeof(buf) - 1] = 0;
  int len = strlen(buf);
  int fd = open(file, O_WRONLY | O_CLOEXEC);
  if (fd == -1)
    return false;
  if (write(fd, buf, len) != len) {
    int err = errno;
    close(fd);
    errno = err;
    return false;
  }
  close(fd);
  return true;
}

static void kill_and_wait(int pid, int* status)
{
  kill(-pid, SIGKILL);
  kill(pid, SIGKILL);
  for (int i = 0; i < 100; i++) {
    if (waitpid(-1, status, WNOHANG | __WALL) == pid)
      return;
    usleep(1000);
  }
  DIR* dir = opendir("/sys/fs/fuse/connections");
  if (dir) {
    for (;;) {
      struct dirent* ent = readdir(dir);
      if (!ent)
        break;
      if (strcmp(ent->d_name, ".") == 0 || strcmp(ent->d_name, "..") == 0)
        continue;
      char abort[300];
      snprintf(abort, sizeof(abort), "/sys/fs/fuse/connections/%s/abort",
               ent->d_name);
      int fd = open(abort, O_WRONLY);
      if (fd == -1) {
        continue;
      }
      if (write(fd, abort, 1) < 0) {
      }
      close(fd);
    }
    closedir(dir);
  } else {
  }
  while (waitpid(-1, status, __WALL) != pid) {
  }
}

static void setup_test()
{
  prctl(PR_SET_PDEATHSIG, SIGKILL, 0, 0, 0);
  setpgrp();
  write_file("/proc/self/oom_score_adj", "1000");
}

struct thread_t {
  int created, call;
  event_t ready, done;
};

static struct thread_t threads[16];
static void execute_call(int call);
static int running;

static void* thr(void* arg)
{
  struct thread_t* th = (struct thread_t*)arg;
  for (;;) {
    event_wait(&th->ready);
    event_reset(&th->ready);
    execute_call(th->call);
    __atomic_fetch_sub(&running, 1, __ATOMIC_RELAXED);
    event_set(&th->done);
  }
  return 0;
}

static void execute_one(void)
{
  int i, call, thread;
  for (call = 0; call < 10; call++) {
    for (thread = 0; thread < (int)(sizeof(threads) / sizeof(threads[0]));
         thread++) {
      struct thread_t* th = &threads[thread];
      if (!th->created) {
        th->created = 1;
        event_init(&th->ready);
        event_init(&th->done);
        event_set(&th->done);
        thread_start(thr, th);
      }
      if (!event_isset(&th->done))
        continue;
      event_reset(&th->done);
      th->call = call;
      __atomic_fetch_add(&running, 1, __ATOMIC_RELAXED);
      event_set(&th->ready);
      event_timedwait(&th->done, 45);
      break;
    }
  }
  for (i = 0; i < 100 && __atomic_load_n(&running, __ATOMIC_RELAXED); i++)
    sleep_ms(1);
}

static void execute_one(void);

#define WAIT_FLAGS __WALL

static void loop(void)
{
  int iter = 0;
  for (;; iter++) {
    int pid = fork();
    if (pid < 0)
      exit(1);
    if (pid == 0) {
      setup_test();
      execute_one();
      exit(0);
    }
    int status = 0;
    uint64_t start = current_time_ms();
    for (;;) {
      if (waitpid(-1, &status, WNOHANG | WAIT_FLAGS) == pid)
        break;
      sleep_ms(1);
      if (current_time_ms() - start < 5 * 1000)
        continue;
      kill_and_wait(pid, &status);
      break;
    }
  }
}

uint64_t r[3] = {0xffffffffffffffff, 0xffffffffffffffff, 0xffffffffffffffff};

void execute_call(int call)
{
  intptr_t res = 0;
  switch (call) {
  case 0:
    res = syscall(__NR_pipe, 0x20000200ul);
    if (res != -1) {
      r[0] = *(uint32_t*)0x20000200;
      r[1] = *(uint32_t*)0x20000204;
    }
    break;
  case 1:
    *(uint64_t*)0x20000840 = 0x20000680;
    memcpy((void*)0x20000680,
           "\x30\xdd\x96\x86\x6c\x86\x19\xc7\x52\x21\x55\xd1\x0e\xfb\x2c\xc5"
           "\xb0\xa7\x61\xc9\x10\x83\xa8\x1d\x84\x83\xc1\xdb\x9a\x56\x74\x4d"
           "\x59\x04\x52\x36\x72\x51\xd7\x0f\x3c\xe7\xbb\xb6\x64\xfe\x97\x5a"
           "\x10\x5c\xad\xf7\x89\x61\x8f\x7a\x51\xab\xdd\xc6\xad\x65\x64\xac"
           "\x6f\x08\xb3\x16\xb1\xde\x15\xf1\xc2\xbb\x3f\x58\x72\x23\x83\xa5"
           "\xa9\xc6\x82\x33\xa3\x9f\xba\x38\xb4\x3f\x87\x1f\xb5\x2e\xab\xc2"
           "\x79\xcf\x67\xbd\x8d\xc1\xcb\xc9\x64\xfe\xce\x03\xff\xd5\x7b\x8a"
           "\x78\x96\x40\x69\x2d\xbb\xb1\x7f\xbe\x53\xbc\xe5\x8c\x46\x1d\x9f"
           "\xae\x31\x7c\xbf\x87\x90\x1a\xed\x08\x0b\x69\x5e\x19\xf7\x66\xbb"
           "\x14\x13\xae\x00\x00\x00\x00\x00\x00\x00\x2d\x1e\x15\x87\x5a\xd5"
           "\xb2\x44\x08\x2f\xac\x18\xc2\x89\x73\xc5\x67\x7b\xa7\x06\xd4\x8f"
           "\x01\x9a\x69\x73\xcc\x21\x27\x8c\x65\x33\xc7\x92\xf2\x92\xe9\x52"
           "\xcc\xbd\x96\xe0\xa9\xc5\x11\x7a\x4d\x9b\x85\xf2\xac\x4c\x72\xde"
           "\xaf\x54\x2c\x33\x69\xa3\x4d\xce\x37\x75\xf8\x49\xbe\x4a\x77\x09"
           "\x59\x80\xf6\xee\xb7\x9a\x17\x98\xd8\xca\x6e\x38\x32\x67\x79\x02"
           "\x19\x91\x7c\x63\x5f\x6d\x3d\xb5\x00\x00\x00\x00\x00\x00\x00\x9e"
           "\x47\xd7\xc7\xf5\xee\x98\xe2\x92\x87\xcf\xb1\xf0\x9d\x4b\xfe\x4a"
           "\x69\x36\xfb\x96\x77\x0f\x51\xd1\x61\xca\x77\x47\x52\xe9\x7b\x4c"
           "\xbb\xa0\xcc\x84\x17\x6c\x00\x95\x86\xbf\xbe\xa7\xfb\x1e\x7b\x9a"
           "\x99\x5c\x6a\x17\x5a\x69\x4a\x30\xa6\x4f\xee\xd3\x73\x01\x51\x50"
           "\x34\x04\x77\x7b\xc6\xb1\xbc\xea\x47\x55\xab\x8a\xac\xc8\x1e\xad"
           "\x62\xeb\xf5\x76\xd1\x34\x45\xd0\x9f\xa5\x10\x03\x3c\x5a\x49\x71"
           "\xb4\x8f\x09\x07\x47\x7c\xee\xaf\x71\x7d\x4d\x42\x9c\x70\x19\x1b"
           "\x65\xa8\xe3\x51\x97\x52\x4b\xd1\x1c\x98\x8e\xff\x77\xad\x06\x89"
           "\x6a\x01\xc4\x97\xc1\x00\x34\xe2",
           392);
    *(uint64_t*)0x20000848 = 0xffffff32;
    syscall(__NR_vmsplice, r[1], 0x20000840ul, 1ul, 0ul);
    break;
  case 2:
    syscall(__NR_close, r[1]);
    break;
  case 3:
    res = syscall(__NR_socket, 0xaul, 1ul, 0);
    if (res != -1)
      r[2] = res;
    break;
  case 4:
    *(uint32_t*)0x200000c0 = 1;
    syscall(__NR_setsockopt, r[2], 6, 0x13, 0x200000c0ul, 4ul);
    break;
  case 5:
    *(uint16_t*)0x20000000 = 0xa;
    *(uint16_t*)0x20000002 = htobe16(0);
    *(uint32_t*)0x20000004 = htobe32(0);
    *(uint64_t*)0x20000008 = htobe64(0);
    *(uint64_t*)0x20000010 = htobe64(1);
    *(uint32_t*)0x20000018 = 0;
    syscall(__NR_connect, r[2], 0x20000000ul, 0x1cul);
    break;
  case 6:
    memcpy((void*)0x20000040, "tls\000", 4);
    syscall(__NR_setsockopt, r[2], 6, 0x1f, 0x20000040ul, 0x355ul);
    break;
  case 7:
    *(uint16_t*)0x20000340 = 0x303;
    *(uint16_t*)0x20000342 = 0x35;
    memcpy((void*)0x20000344, "\x25\x54\xa4\x85\xdb\x0c\x30\xf2", 8);
    memcpy((void*)0x2000034c,
           "\x42\x91\xa3\x9c\x86\x8e\x90\xa3\x75\xe7\xe2\x50\x7c\x31\x14\x7d",
           16);
    memcpy((void*)0x2000035c, "\x51\x40\xff\x97", 4);
    memcpy((void*)0x20000360, "\xeb\x92\x42\xec\xb4\x75\x5b\xe6", 8);
    syscall(__NR_setsockopt, r[1], 0x11a, 1, 0x20000340ul, 0x28ul);
    break;
  case 8:
    syscall(__NR_splice, r[0], 0ul, r[1], 0ul, 0x80000fffffffcul, 0ul);
    break;
  case 9:
    *(uint64_t*)0x20000180 = 0;
    *(uint32_t*)0x20000188 = 0;
    *(uint64_t*)0x20000190 = 0x20000140;
    *(uint64_t*)0x20000140 = 0x20000100;
    *(uint32_t*)0x20000100 = 0x10;
    *(uint16_t*)0x20000104 = 0x3f5;
    *(uint16_t*)0x20000106 = 0x20;
    *(uint32_t*)0x20000108 = 0x70bd2d;
    *(uint32_t*)0x2000010c = 0x25dfdbfe;
    *(uint64_t*)0x20000148 = 0x10;
    *(uint64_t*)0x20000198 = 1;
    *(uint64_t*)0x200001a0 = 0;
    *(uint64_t*)0x200001a8 = 0;
    *(uint32_t*)0x200001b0 = 0x20000000;
    syscall(__NR_sendmsg, r[1], 0x20000180ul, 0x8040ul);
    break;
  }
}
int main(void)
{
  syscall(__NR_mmap, 0x1ffff000ul, 0x1000ul, 0ul, 0x32ul, -1, 0ul);
  syscall(__NR_mmap, 0x20000000ul, 0x1000000ul, 7ul, 0x32ul, -1, 0ul);
  syscall(__NR_mmap, 0x21000000ul, 0x1000ul, 0ul, 0x32ul, -1, 0ul);
  loop();
  return 0;
}
