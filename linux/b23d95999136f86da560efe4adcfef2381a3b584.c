// BUG: bad unlock balance in ipmr_mfc_seq_stop
// https://syzkaller.appspot.com/bug?id=b23d95999136f86da560efe4adcfef2381a3b584
// status:fixed
// autogenerated by syzkaller (http://github.com/google/syzkaller)

#define _GNU_SOURCE
#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <linux/futex.h>
#include <pthread.h>
#include <signal.h>
#include <stdarg.h>
#include <stdio.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/prctl.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/time.h>
#include <sys/wait.h>
#include <time.h>
#include <unistd.h>

__attribute__((noreturn)) static void doexit(int status)
{
  volatile unsigned i;
  syscall(__NR_exit_group, status);
  for (i = 0;; i++) {
  }
}
#include <setjmp.h>
#include <signal.h>
#include <stdint.h>
#include <string.h>
#include <string.h>

const int kFailStatus = 67;
const int kRetryStatus = 69;

static void fail(const char* msg, ...)
{
  int e = errno;
  va_list args;
  va_start(args, msg);
  vfprintf(stderr, msg, args);
  va_end(args);
  fprintf(stderr, " (errno %d)\n", e);
  doexit((e == ENOMEM || e == EAGAIN) ? kRetryStatus : kFailStatus);
}

static __thread int skip_segv;
static __thread jmp_buf segv_env;

static void segv_handler(int sig, siginfo_t* info, void* uctx)
{
  uintptr_t addr = (uintptr_t)info->si_addr;
  const uintptr_t prog_start = 1 << 20;
  const uintptr_t prog_end = 100 << 20;
  if (__atomic_load_n(&skip_segv, __ATOMIC_RELAXED) &&
      (addr < prog_start || addr > prog_end)) {
    _longjmp(segv_env, 1);
  }
  doexit(sig);
}

static void install_segv_handler()
{
  struct sigaction sa;

  memset(&sa, 0, sizeof(sa));
  sa.sa_handler = SIG_IGN;
  syscall(SYS_rt_sigaction, 0x20, &sa, NULL, 8);
  syscall(SYS_rt_sigaction, 0x21, &sa, NULL, 8);

  memset(&sa, 0, sizeof(sa));
  sa.sa_sigaction = segv_handler;
  sa.sa_flags = SA_NODEFER | SA_SIGINFO;
  sigaction(SIGSEGV, &sa, NULL);
  sigaction(SIGBUS, &sa, NULL);
}

#define NONFAILING(...)                                                        \
  {                                                                            \
    __atomic_fetch_add(&skip_segv, 1, __ATOMIC_SEQ_CST);                       \
    if (_setjmp(segv_env) == 0) {                                              \
      __VA_ARGS__;                                                             \
    }                                                                          \
    __atomic_fetch_sub(&skip_segv, 1, __ATOMIC_SEQ_CST);                       \
  }

static uint64_t current_time_ms()
{
  struct timespec ts;

  if (clock_gettime(CLOCK_MONOTONIC, &ts))
    fail("clock_gettime failed");
  return (uint64_t)ts.tv_sec * 1000 + (uint64_t)ts.tv_nsec / 1000000;
}

static uintptr_t syz_open_procfs(uintptr_t a0, uintptr_t a1)
{

  char buf[128];
  memset(buf, 0, sizeof(buf));
  if (a0 == 0) {
    NONFAILING(snprintf(buf, sizeof(buf), "/proc/self/%s", (char*)a1));
  } else if (a0 == (uintptr_t)-1) {
    NONFAILING(snprintf(buf, sizeof(buf), "/proc/thread-self/%s", (char*)a1));
  } else {
    NONFAILING(snprintf(buf, sizeof(buf), "/proc/self/task/%d/%s", (int)a0,
                        (char*)a1));
  }
  int fd = open(buf, O_RDWR);
  if (fd == -1)
    fd = open(buf, O_RDONLY);
  return fd;
}

static void test();

void loop()
{
  int iter;
  for (iter = 0;; iter++) {
    int pid = fork();
    if (pid < 0)
      fail("loop fork failed");
    if (pid == 0) {
      prctl(PR_SET_PDEATHSIG, SIGKILL, 0, 0, 0);
      setpgrp();
      test();
      doexit(0);
    }
    int status = 0;
    uint64_t start = current_time_ms();
    for (;;) {
      int res = waitpid(-1, &status, __WALL | WNOHANG);
      if (res == pid)
        break;
      usleep(1000);
      if (current_time_ms() - start > 5 * 1000) {
        kill(-pid, SIGKILL);
        kill(pid, SIGKILL);
        while (waitpid(-1, &status, __WALL) != pid) {
        }
        break;
      }
    }
  }
}

struct thread_t {
  int created, running, call;
  pthread_t th;
};

static struct thread_t threads[16];
static void execute_call(int call);
static int running;
static int collide;

static void* thr(void* arg)
{
  struct thread_t* th = (struct thread_t*)arg;
  for (;;) {
    while (!__atomic_load_n(&th->running, __ATOMIC_ACQUIRE))
      syscall(SYS_futex, &th->running, FUTEX_WAIT, 0, 0);
    execute_call(th->call);
    __atomic_fetch_sub(&running, 1, __ATOMIC_RELAXED);
    __atomic_store_n(&th->running, 0, __ATOMIC_RELEASE);
    syscall(SYS_futex, &th->running, FUTEX_WAKE);
  }
  return 0;
}

static void execute(int num_calls)
{
  int call, thread;
  running = 0;
  for (call = 0; call < num_calls; call++) {
    for (thread = 0; thread < sizeof(threads) / sizeof(threads[0]); thread++) {
      struct thread_t* th = &threads[thread];
      if (!th->created) {
        th->created = 1;
        pthread_attr_t attr;
        pthread_attr_init(&attr);
        pthread_attr_setstacksize(&attr, 128 << 10);
        pthread_create(&th->th, &attr, thr, th);
      }
      if (!__atomic_load_n(&th->running, __ATOMIC_ACQUIRE)) {
        th->call = call;
        __atomic_fetch_add(&running, 1, __ATOMIC_RELAXED);
        __atomic_store_n(&th->running, 1, __ATOMIC_RELEASE);
        syscall(SYS_futex, &th->running, FUTEX_WAKE);
        if (collide && call % 2)
          break;
        struct timespec ts;
        ts.tv_sec = 0;
        ts.tv_nsec = 20 * 1000 * 1000;
        syscall(SYS_futex, &th->running, FUTEX_WAIT, 1, &ts);
        if (running)
          usleep((call == num_calls - 1) ? 10000 : 1000);
        break;
      }
    }
  }
}

#ifndef __NR_mmap
#define __NR_mmap 192
#endif
#ifndef __NR_pread64
#define __NR_pread64 180
#endif
#ifndef __NR_socket
#define __NR_socket 359
#endif
#ifndef __NR_setsockopt
#define __NR_setsockopt 366
#endif
#undef __NR_mmap
#define __NR_mmap __NR_mmap2

long r[2];
void execute_call(int call)
{
  switch (call) {
  case 0:
    syscall(__NR_mmap, 0x20000000, 0xfff000, 3, 0x32, -1, 0);
    break;
  case 1:
    NONFAILING(memcpy((void*)0x20bb8000, "net/ip6_mr_cache", 17));
    r[0] = syz_open_procfs(0, 0x20bb8000);
    break;
  case 2:
    syscall(__NR_pread64, r[0], 0x20caef7b, 0x1c, 0x4c);
    break;
  case 3:
    r[1] = syscall(__NR_socket, 0xa, 2, 0);
    break;
  case 4:
    NONFAILING(*(uint16_t*)0x206a4fa4 = 0xa);
    NONFAILING(*(uint16_t*)0x206a4fa6 = htobe16(0x4e20));
    NONFAILING(*(uint32_t*)0x206a4fa8 = 0);
    NONFAILING(*(uint8_t*)0x206a4fac = 0xfe);
    NONFAILING(*(uint8_t*)0x206a4fad = 0x80);
    NONFAILING(*(uint8_t*)0x206a4fae = 0);
    NONFAILING(*(uint8_t*)0x206a4faf = 0);
    NONFAILING(*(uint8_t*)0x206a4fb0 = 0);
    NONFAILING(*(uint8_t*)0x206a4fb1 = 0);
    NONFAILING(*(uint8_t*)0x206a4fb2 = 0);
    NONFAILING(*(uint8_t*)0x206a4fb3 = 0);
    NONFAILING(*(uint8_t*)0x206a4fb4 = 0);
    NONFAILING(*(uint8_t*)0x206a4fb5 = 0);
    NONFAILING(*(uint8_t*)0x206a4fb6 = 0);
    NONFAILING(*(uint8_t*)0x206a4fb7 = 0);
    NONFAILING(*(uint8_t*)0x206a4fb8 = 0);
    NONFAILING(*(uint8_t*)0x206a4fb9 = 0);
    NONFAILING(*(uint8_t*)0x206a4fba = 0);
    NONFAILING(*(uint8_t*)0x206a4fbb = 0xaa);
    NONFAILING(*(uint32_t*)0x206a4fbc = 0);
    NONFAILING(*(uint16_t*)0x206a4fc0 = 0xa);
    NONFAILING(*(uint16_t*)0x206a4fc2 = htobe16(0x4e20));
    NONFAILING(*(uint32_t*)0x206a4fc4 = 0);
    NONFAILING(*(uint8_t*)0x206a4fc8 = 0);
    NONFAILING(*(uint8_t*)0x206a4fc9 = 0);
    NONFAILING(*(uint8_t*)0x206a4fca = 0);
    NONFAILING(*(uint8_t*)0x206a4fcb = 0);
    NONFAILING(*(uint8_t*)0x206a4fcc = 0);
    NONFAILING(*(uint8_t*)0x206a4fcd = 0);
    NONFAILING(*(uint8_t*)0x206a4fce = 0);
    NONFAILING(*(uint8_t*)0x206a4fcf = 0);
    NONFAILING(*(uint8_t*)0x206a4fd0 = 0);
    NONFAILING(*(uint8_t*)0x206a4fd1 = 0);
    NONFAILING(*(uint8_t*)0x206a4fd2 = 0);
    NONFAILING(*(uint8_t*)0x206a4fd3 = 0);
    NONFAILING(*(uint8_t*)0x206a4fd4 = 0);
    NONFAILING(*(uint8_t*)0x206a4fd5 = 0);
    NONFAILING(*(uint8_t*)0x206a4fd6 = 0);
    NONFAILING(*(uint8_t*)0x206a4fd7 = 0);
    NONFAILING(*(uint32_t*)0x206a4fd8 = 0);
    NONFAILING(*(uint16_t*)0x206a4fdc = 0);
    NONFAILING(*(uint32_t*)0x206a4fe0 = 0xf67);
    NONFAILING(*(uint32_t*)0x206a4fe4 = 0);
    NONFAILING(*(uint32_t*)0x206a4fe8 = 0);
    NONFAILING(*(uint32_t*)0x206a4fec = 0);
    NONFAILING(*(uint32_t*)0x206a4ff0 = 0);
    NONFAILING(*(uint32_t*)0x206a4ff4 = 0);
    NONFAILING(*(uint32_t*)0x206a4ff8 = 0);
    NONFAILING(*(uint32_t*)0x206a4ffc = 0);
    syscall(__NR_setsockopt, r[1], 0x29, 0xd2, 0x206a4fa4, 0x5c);
    break;
  }
}

void test()
{
  memset(r, -1, sizeof(r));
  execute(5);
  collide = 1;
  execute(5);
}

int main()
{
  install_segv_handler();
  for (;;) {
    loop();
  }
}
