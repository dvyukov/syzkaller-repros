// WARNING in qt2_attach/usb_submit_urb
// https://syzkaller.appspot.com/bug?id=19acce48d8344df49b3a
// status:0
// autogenerated by syzkaller (https://github.com/google/syzkaller)

#define _GNU_SOURCE

#include <dirent.h>
#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <signal.h>
#include <stdarg.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mount.h>
#include <sys/prctl.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <time.h>
#include <unistd.h>

#include <linux/usb/ch9.h>

static unsigned long long procid;

static void sleep_ms(uint64_t ms)
{
  usleep(ms * 1000);
}

static uint64_t current_time_ms(void)
{
  struct timespec ts;
  if (clock_gettime(CLOCK_MONOTONIC, &ts))
    exit(1);
  return (uint64_t)ts.tv_sec * 1000 + (uint64_t)ts.tv_nsec / 1000000;
}

static bool write_file(const char* file, const char* what, ...)
{
  char buf[1024];
  va_list args;
  va_start(args, what);
  vsnprintf(buf, sizeof(buf), what, args);
  va_end(args);
  buf[sizeof(buf) - 1] = 0;
  int len = strlen(buf);
  int fd = open(file, O_WRONLY | O_CLOEXEC);
  if (fd == -1)
    return false;
  if (write(fd, buf, len) != len) {
    int err = errno;
    close(fd);
    errno = err;
    return false;
  }
  close(fd);
  return true;
}

#define MAX_FDS 30

#define USB_MAX_IFACE_NUM 4
#define USB_MAX_EP_NUM 32
#define USB_MAX_FDS 6

struct usb_endpoint_index {
  struct usb_endpoint_descriptor desc;
  int handle;
};

struct usb_iface_index {
  struct usb_interface_descriptor* iface;
  uint8_t bInterfaceNumber;
  uint8_t bAlternateSetting;
  uint8_t bInterfaceClass;
  struct usb_endpoint_index eps[USB_MAX_EP_NUM];
  int eps_num;
};

struct usb_device_index {
  struct usb_device_descriptor* dev;
  struct usb_config_descriptor* config;
  uint8_t bDeviceClass;
  uint8_t bMaxPower;
  int config_length;
  struct usb_iface_index ifaces[USB_MAX_IFACE_NUM];
  int ifaces_num;
  int iface_cur;
};

struct usb_info {
  int fd;
  struct usb_device_index index;
};

static struct usb_info usb_devices[USB_MAX_FDS];
static int usb_devices_num;

static bool parse_usb_descriptor(const char* buffer, size_t length,
                                 struct usb_device_index* index)
{
  if (length < sizeof(*index->dev) + sizeof(*index->config))
    return false;
  memset(index, 0, sizeof(*index));
  index->dev = (struct usb_device_descriptor*)buffer;
  index->config = (struct usb_config_descriptor*)(buffer + sizeof(*index->dev));
  index->bDeviceClass = index->dev->bDeviceClass;
  index->bMaxPower = index->config->bMaxPower;
  index->config_length = length - sizeof(*index->dev);
  index->iface_cur = -1;
  size_t offset = 0;
  while (true) {
    if (offset + 1 >= length)
      break;
    uint8_t desc_length = buffer[offset];
    uint8_t desc_type = buffer[offset + 1];
    if (desc_length <= 2)
      break;
    if (offset + desc_length > length)
      break;
    if (desc_type == USB_DT_INTERFACE &&
        index->ifaces_num < USB_MAX_IFACE_NUM) {
      struct usb_interface_descriptor* iface =
          (struct usb_interface_descriptor*)(buffer + offset);
      index->ifaces[index->ifaces_num].iface = iface;
      index->ifaces[index->ifaces_num].bInterfaceNumber =
          iface->bInterfaceNumber;
      index->ifaces[index->ifaces_num].bAlternateSetting =
          iface->bAlternateSetting;
      index->ifaces[index->ifaces_num].bInterfaceClass = iface->bInterfaceClass;
      index->ifaces_num++;
    }
    if (desc_type == USB_DT_ENDPOINT && index->ifaces_num > 0) {
      struct usb_iface_index* iface = &index->ifaces[index->ifaces_num - 1];
      if (iface->eps_num < USB_MAX_EP_NUM) {
        memcpy(&iface->eps[iface->eps_num].desc, buffer + offset,
               sizeof(iface->eps[iface->eps_num].desc));
        iface->eps_num++;
      }
    }
    offset += desc_length;
  }
  return true;
}

static struct usb_device_index* add_usb_index(int fd, const char* dev,
                                              size_t dev_len)
{
  int i = __atomic_fetch_add(&usb_devices_num, 1, __ATOMIC_RELAXED);
  if (i >= USB_MAX_FDS)
    return NULL;
  if (!parse_usb_descriptor(dev, dev_len, &usb_devices[i].index))
    return NULL;
  __atomic_store_n(&usb_devices[i].fd, fd, __ATOMIC_RELEASE);
  return &usb_devices[i].index;
}

static struct usb_device_index* lookup_usb_index(int fd)
{
  for (int i = 0; i < USB_MAX_FDS; i++) {
    if (__atomic_load_n(&usb_devices[i].fd, __ATOMIC_ACQUIRE) == fd) {
      return &usb_devices[i].index;
    }
  }
  return NULL;
}

struct vusb_connect_string_descriptor {
  uint32_t len;
  char* str;
} __attribute__((packed));

struct vusb_connect_descriptors {
  uint32_t qual_len;
  char* qual;
  uint32_t bos_len;
  char* bos;
  uint32_t strs_len;
  struct vusb_connect_string_descriptor strs[0];
} __attribute__((packed));

static const char default_string[] = {8, USB_DT_STRING, 's', 0, 'y', 0, 'z', 0};

static const char default_lang_id[] = {4, USB_DT_STRING, 0x09, 0x04};

static bool
lookup_connect_response_in(int fd, const struct vusb_connect_descriptors* descs,
                           const struct usb_ctrlrequest* ctrl,
                           char** response_data, uint32_t* response_length)
{
  struct usb_device_index* index = lookup_usb_index(fd);
  uint8_t str_idx;
  if (!index)
    return false;
  switch (ctrl->bRequestType & USB_TYPE_MASK) {
  case USB_TYPE_STANDARD:
    switch (ctrl->bRequest) {
    case USB_REQ_GET_DESCRIPTOR:
      switch (ctrl->wValue >> 8) {
      case USB_DT_DEVICE:
        *response_data = (char*)index->dev;
        *response_length = sizeof(*index->dev);
        return true;
      case USB_DT_CONFIG:
        *response_data = (char*)index->config;
        *response_length = index->config_length;
        return true;
      case USB_DT_STRING:
        str_idx = (uint8_t)ctrl->wValue;
        if (descs && str_idx < descs->strs_len) {
          *response_data = descs->strs[str_idx].str;
          *response_length = descs->strs[str_idx].len;
          return true;
        }
        if (str_idx == 0) {
          *response_data = (char*)&default_lang_id[0];
          *response_length = default_lang_id[0];
          return true;
        }
        *response_data = (char*)&default_string[0];
        *response_length = default_string[0];
        return true;
      case USB_DT_BOS:
        *response_data = descs->bos;
        *response_length = descs->bos_len;
        return true;
      case USB_DT_DEVICE_QUALIFIER:
        if (!descs->qual) {
          struct usb_qualifier_descriptor* qual =
              (struct usb_qualifier_descriptor*)response_data;
          qual->bLength = sizeof(*qual);
          qual->bDescriptorType = USB_DT_DEVICE_QUALIFIER;
          qual->bcdUSB = index->dev->bcdUSB;
          qual->bDeviceClass = index->dev->bDeviceClass;
          qual->bDeviceSubClass = index->dev->bDeviceSubClass;
          qual->bDeviceProtocol = index->dev->bDeviceProtocol;
          qual->bMaxPacketSize0 = index->dev->bMaxPacketSize0;
          qual->bNumConfigurations = index->dev->bNumConfigurations;
          qual->bRESERVED = 0;
          *response_length = sizeof(*qual);
          return true;
        }
        *response_data = descs->qual;
        *response_length = descs->qual_len;
        return true;
      default:
        break;
      }
      break;
    default:
      break;
    }
    break;
  default:
    break;
  }
  return false;
}

typedef bool (*lookup_connect_out_response_t)(
    int fd, const struct vusb_connect_descriptors* descs,
    const struct usb_ctrlrequest* ctrl, bool* done);

static bool lookup_connect_response_out_generic(
    int fd, const struct vusb_connect_descriptors* descs,
    const struct usb_ctrlrequest* ctrl, bool* done)
{
  switch (ctrl->bRequestType & USB_TYPE_MASK) {
  case USB_TYPE_STANDARD:
    switch (ctrl->bRequest) {
    case USB_REQ_SET_CONFIGURATION:
      *done = true;
      return true;
    default:
      break;
    }
    break;
  }
  return false;
}

#define UDC_NAME_LENGTH_MAX 128

struct usb_raw_init {
  __u8 driver_name[UDC_NAME_LENGTH_MAX];
  __u8 device_name[UDC_NAME_LENGTH_MAX];
  __u8 speed;
};

enum usb_raw_event_type {
  USB_RAW_EVENT_INVALID = 0,
  USB_RAW_EVENT_CONNECT = 1,
  USB_RAW_EVENT_CONTROL = 2,
};

struct usb_raw_event {
  __u32 type;
  __u32 length;
  __u8 data[0];
};

struct usb_raw_ep_io {
  __u16 ep;
  __u16 flags;
  __u32 length;
  __u8 data[0];
};

#define USB_RAW_EPS_NUM_MAX 30
#define USB_RAW_EP_NAME_MAX 16
#define USB_RAW_EP_ADDR_ANY 0xff

struct usb_raw_ep_caps {
  __u32 type_control : 1;
  __u32 type_iso : 1;
  __u32 type_bulk : 1;
  __u32 type_int : 1;
  __u32 dir_in : 1;
  __u32 dir_out : 1;
};

struct usb_raw_ep_limits {
  __u16 maxpacket_limit;
  __u16 max_streams;
  __u32 reserved;
};

struct usb_raw_ep_info {
  __u8 name[USB_RAW_EP_NAME_MAX];
  __u32 addr;
  struct usb_raw_ep_caps caps;
  struct usb_raw_ep_limits limits;
};

struct usb_raw_eps_info {
  struct usb_raw_ep_info eps[USB_RAW_EPS_NUM_MAX];
};

#define USB_RAW_IOCTL_INIT _IOW('U', 0, struct usb_raw_init)
#define USB_RAW_IOCTL_RUN _IO('U', 1)
#define USB_RAW_IOCTL_EVENT_FETCH _IOR('U', 2, struct usb_raw_event)
#define USB_RAW_IOCTL_EP0_WRITE _IOW('U', 3, struct usb_raw_ep_io)
#define USB_RAW_IOCTL_EP0_READ _IOWR('U', 4, struct usb_raw_ep_io)
#define USB_RAW_IOCTL_EP_ENABLE _IOW('U', 5, struct usb_endpoint_descriptor)
#define USB_RAW_IOCTL_EP_DISABLE _IOW('U', 6, __u32)
#define USB_RAW_IOCTL_EP_WRITE _IOW('U', 7, struct usb_raw_ep_io)
#define USB_RAW_IOCTL_EP_READ _IOWR('U', 8, struct usb_raw_ep_io)
#define USB_RAW_IOCTL_CONFIGURE _IO('U', 9)
#define USB_RAW_IOCTL_VBUS_DRAW _IOW('U', 10, __u32)
#define USB_RAW_IOCTL_EPS_INFO _IOR('U', 11, struct usb_raw_eps_info)
#define USB_RAW_IOCTL_EP0_STALL _IO('U', 12)
#define USB_RAW_IOCTL_EP_SET_HALT _IOW('U', 13, __u32)
#define USB_RAW_IOCTL_EP_CLEAR_HALT _IOW('U', 14, __u32)
#define USB_RAW_IOCTL_EP_SET_WEDGE _IOW('U', 15, __u32)

static int usb_raw_open()
{
  return open("/dev/raw-gadget", O_RDWR);
}

static int usb_raw_init(int fd, uint32_t speed, const char* driver,
                        const char* device)
{
  struct usb_raw_init arg;
  strncpy((char*)&arg.driver_name[0], driver, sizeof(arg.driver_name));
  strncpy((char*)&arg.device_name[0], device, sizeof(arg.device_name));
  arg.speed = speed;
  return ioctl(fd, USB_RAW_IOCTL_INIT, &arg);
}

static int usb_raw_run(int fd)
{
  return ioctl(fd, USB_RAW_IOCTL_RUN, 0);
}

static int usb_raw_event_fetch(int fd, struct usb_raw_event* event)
{
  return ioctl(fd, USB_RAW_IOCTL_EVENT_FETCH, event);
}

static int usb_raw_ep0_write(int fd, struct usb_raw_ep_io* io)
{
  return ioctl(fd, USB_RAW_IOCTL_EP0_WRITE, io);
}

static int usb_raw_ep0_read(int fd, struct usb_raw_ep_io* io)
{
  return ioctl(fd, USB_RAW_IOCTL_EP0_READ, io);
}

static int usb_raw_ep_enable(int fd, struct usb_endpoint_descriptor* desc)
{
  return ioctl(fd, USB_RAW_IOCTL_EP_ENABLE, desc);
}

static int usb_raw_ep_disable(int fd, int ep)
{
  return ioctl(fd, USB_RAW_IOCTL_EP_DISABLE, ep);
}

static int usb_raw_configure(int fd)
{
  return ioctl(fd, USB_RAW_IOCTL_CONFIGURE, 0);
}

static int usb_raw_vbus_draw(int fd, uint32_t power)
{
  return ioctl(fd, USB_RAW_IOCTL_VBUS_DRAW, power);
}

static int usb_raw_ep0_stall(int fd)
{
  return ioctl(fd, USB_RAW_IOCTL_EP0_STALL, 0);
}

static void set_interface(int fd, int n)
{
  struct usb_device_index* index = lookup_usb_index(fd);
  if (!index)
    return;
  if (index->iface_cur >= 0 && index->iface_cur < index->ifaces_num) {
    for (int ep = 0; ep < index->ifaces[index->iface_cur].eps_num; ep++) {
      int rv = usb_raw_ep_disable(
          fd, index->ifaces[index->iface_cur].eps[ep].handle);
      if (rv < 0) {
      } else {
      }
    }
  }
  if (n >= 0 && n < index->ifaces_num) {
    for (int ep = 0; ep < index->ifaces[n].eps_num; ep++) {
      int rv = usb_raw_ep_enable(fd, &index->ifaces[n].eps[ep].desc);
      if (rv < 0) {
      } else {
        index->ifaces[n].eps[ep].handle = rv;
      }
    }
    index->iface_cur = n;
  }
}

static int configure_device(int fd)
{
  struct usb_device_index* index = lookup_usb_index(fd);
  if (!index)
    return -1;
  int rv = usb_raw_vbus_draw(fd, index->bMaxPower);
  if (rv < 0) {
    return rv;
  }
  rv = usb_raw_configure(fd);
  if (rv < 0) {
    return rv;
  }
  set_interface(fd, 0);
  return 0;
}

#define USB_MAX_PACKET_SIZE 4096

struct usb_raw_control_event {
  struct usb_raw_event inner;
  struct usb_ctrlrequest ctrl;
  char data[USB_MAX_PACKET_SIZE];
};

struct usb_raw_ep_io_data {
  struct usb_raw_ep_io inner;
  char data[USB_MAX_PACKET_SIZE];
};

static volatile long
syz_usb_connect_impl(uint64_t speed, uint64_t dev_len, const char* dev,
                     const struct vusb_connect_descriptors* descs,
                     lookup_connect_out_response_t lookup_connect_response_out)
{
  if (!dev) {
    return -1;
  }
  int fd = usb_raw_open();
  if (fd < 0) {
    return fd;
  }
  if (fd >= MAX_FDS) {
    close(fd);
    return -1;
  }
  struct usb_device_index* index = add_usb_index(fd, dev, dev_len);
  if (!index) {
    return -1;
  }
  char device[32];
  sprintf(&device[0], "dummy_udc.%llu", procid);
  int rv = usb_raw_init(fd, speed, "dummy_udc", &device[0]);
  if (rv < 0) {
    return rv;
  }
  rv = usb_raw_run(fd);
  if (rv < 0) {
    return rv;
  }
  bool done = false;
  while (!done) {
    struct usb_raw_control_event event;
    event.inner.type = 0;
    event.inner.length = sizeof(event.ctrl);
    rv = usb_raw_event_fetch(fd, (struct usb_raw_event*)&event);
    if (rv < 0) {
      return rv;
    }
    if (event.inner.type != USB_RAW_EVENT_CONTROL)
      continue;
    char* response_data = NULL;
    uint32_t response_length = 0;
    if (event.ctrl.bRequestType & USB_DIR_IN) {
      if (!lookup_connect_response_in(fd, descs, &event.ctrl, &response_data,
                                      &response_length)) {
        usb_raw_ep0_stall(fd);
        continue;
      }
    } else {
      if (!lookup_connect_response_out(fd, descs, &event.ctrl, &done)) {
        usb_raw_ep0_stall(fd);
        continue;
      }
      response_data = NULL;
      response_length = event.ctrl.wLength;
    }
    if ((event.ctrl.bRequestType & USB_TYPE_MASK) == USB_TYPE_STANDARD &&
        event.ctrl.bRequest == USB_REQ_SET_CONFIGURATION) {
      rv = configure_device(fd);
      if (rv < 0) {
        return rv;
      }
    }
    struct usb_raw_ep_io_data response;
    response.inner.ep = 0;
    response.inner.flags = 0;
    if (response_length > sizeof(response.data))
      response_length = 0;
    if (event.ctrl.wLength < response_length)
      response_length = event.ctrl.wLength;
    response.inner.length = response_length;
    if (response_data)
      memcpy(&response.data[0], response_data, response_length);
    else
      memset(&response.data[0], 0, response_length);
    if (event.ctrl.bRequestType & USB_DIR_IN) {
      rv = usb_raw_ep0_write(fd, (struct usb_raw_ep_io*)&response);
    } else {
      rv = usb_raw_ep0_read(fd, (struct usb_raw_ep_io*)&response);
    }
    if (rv < 0) {
      return rv;
    }
  }
  sleep_ms(200);
  return fd;
}

static volatile long syz_usb_connect(volatile long a0, volatile long a1,
                                     volatile long a2, volatile long a3)
{
  uint64_t speed = a0;
  uint64_t dev_len = a1;
  const char* dev = (const char*)a2;
  const struct vusb_connect_descriptors* descs =
      (const struct vusb_connect_descriptors*)a3;
  return syz_usb_connect_impl(speed, dev_len, dev, descs,
                              &lookup_connect_response_out_generic);
}

static void kill_and_wait(int pid, int* status)
{
  kill(-pid, SIGKILL);
  kill(pid, SIGKILL);
  for (int i = 0; i < 100; i++) {
    if (waitpid(-1, status, WNOHANG | __WALL) == pid)
      return;
    usleep(1000);
  }
  DIR* dir = opendir("/sys/fs/fuse/connections");
  if (dir) {
    for (;;) {
      struct dirent* ent = readdir(dir);
      if (!ent)
        break;
      if (strcmp(ent->d_name, ".") == 0 || strcmp(ent->d_name, "..") == 0)
        continue;
      char abort[300];
      snprintf(abort, sizeof(abort), "/sys/fs/fuse/connections/%s/abort",
               ent->d_name);
      int fd = open(abort, O_WRONLY);
      if (fd == -1) {
        continue;
      }
      if (write(fd, abort, 1) < 0) {
      }
      close(fd);
    }
    closedir(dir);
  } else {
  }
  while (waitpid(-1, status, __WALL) != pid) {
  }
}

static void setup_test()
{
  prctl(PR_SET_PDEATHSIG, SIGKILL, 0, 0, 0);
  setpgrp();
  write_file("/proc/self/oom_score_adj", "1000");
}

static void execute_one(void);

#define WAIT_FLAGS __WALL

static void loop(void)
{
  int iter = 0;
  for (;; iter++) {
    int pid = fork();
    if (pid < 0)
      exit(1);
    if (pid == 0) {
      setup_test();
      execute_one();
      exit(0);
    }
    int status = 0;
    uint64_t start = current_time_ms();
    for (;;) {
      if (waitpid(-1, &status, WNOHANG | WAIT_FLAGS) == pid)
        break;
      sleep_ms(1);
      if (current_time_ms() - start < 5000) {
        continue;
      }
      kill_and_wait(pid, &status);
      break;
    }
  }
}

void execute_one(void)
{
  *(uint8_t*)0x20000340 = 0x12;
  *(uint8_t*)0x20000341 = 1;
  *(uint16_t*)0x20000342 = 0x250;
  *(uint8_t*)0x20000344 = 0xbc;
  *(uint8_t*)0x20000345 = 0x9e;
  *(uint8_t*)0x20000346 = 0xac;
  *(uint8_t*)0x20000347 = 8;
  *(uint16_t*)0x20000348 = 0x61d;
  *(uint16_t*)0x2000034a = 0xc170;
  *(uint16_t*)0x2000034c = 0x1141;
  *(uint8_t*)0x2000034e = 1;
  *(uint8_t*)0x2000034f = 2;
  *(uint8_t*)0x20000350 = 3;
  *(uint8_t*)0x20000351 = 1;
  *(uint8_t*)0x20000352 = 9;
  *(uint8_t*)0x20000353 = 2;
  *(uint16_t*)0x20000354 = 0xf3;
  *(uint8_t*)0x20000356 = 4;
  *(uint8_t*)0x20000357 = 0x1f;
  *(uint8_t*)0x20000358 = 7;
  *(uint8_t*)0x20000359 = 0x80;
  *(uint8_t*)0x2000035a = 2;
  *(uint8_t*)0x2000035b = 9;
  *(uint8_t*)0x2000035c = 4;
  *(uint8_t*)0x2000035d = 0xe0;
  *(uint8_t*)0x2000035e = 0x17;
  *(uint8_t*)0x2000035f = 0;
  *(uint8_t*)0x20000360 = 0x16;
  *(uint8_t*)0x20000361 = 0xc1;
  *(uint8_t*)0x20000362 = 0xba;
  *(uint8_t*)0x20000363 = 6;
  *(uint8_t*)0x20000364 = 9;
  *(uint8_t*)0x20000365 = 0x24;
  *(uint8_t*)0x20000366 = 2;
  *(uint8_t*)0x20000367 = 2;
  *(uint16_t*)0x20000368 = 5;
  *(uint16_t*)0x2000036a = 0x3fe;
  *(uint8_t*)0x2000036c = 0xbe;
  *(uint8_t*)0x2000036d = 8;
  *(uint8_t*)0x2000036e = 0x24;
  *(uint8_t*)0x2000036f = 2;
  *(uint8_t*)0x20000370 = 1;
  *(uint8_t*)0x20000371 = 2;
  *(uint8_t*)0x20000372 = 2;
  *(uint8_t*)0x20000373 = 2;
  *(uint8_t*)0x20000374 = 0x89;
  *(uint8_t*)0x20000375 = 8;
  *(uint8_t*)0x20000376 = 0x24;
  *(uint8_t*)0x20000377 = 2;
  *(uint8_t*)0x20000378 = 1;
  *(uint8_t*)0x20000379 = 5;
  *(uint8_t*)0x2000037a = 3;
  *(uint8_t*)0x2000037b = 0x81;
  *(uint8_t*)0x2000037c = 9;
  *(uint8_t*)0x2000037d = 9;
  *(uint8_t*)0x2000037e = 0x24;
  *(uint8_t*)0x2000037f = 2;
  *(uint8_t*)0x20000380 = 2;
  *(uint16_t*)0x20000381 = 8;
  *(uint16_t*)0x20000383 = 0x1ff;
  *(uint8_t*)0x20000385 = 5;
  *(uint8_t*)0x20000386 = 9;
  *(uint8_t*)0x20000387 = 0x24;
  *(uint8_t*)0x20000388 = 2;
  *(uint8_t*)0x20000389 = 2;
  *(uint16_t*)0x2000038a = 7;
  *(uint16_t*)0x2000038c = 9;
  *(uint8_t*)0x2000038e = 0x1f;
  *(uint8_t*)0x2000038f = 7;
  *(uint8_t*)0x20000390 = 0x24;
  *(uint8_t*)0x20000391 = 1;
  *(uint8_t*)0x20000392 = 0xe8;
  *(uint8_t*)0x20000393 = 9;
  *(uint16_t*)0x20000394 = 1;
  *(uint8_t*)0x20000396 = 5;
  *(uint8_t*)0x20000397 = 0x24;
  *(uint8_t*)0x20000398 = 6;
  *(uint8_t*)0x20000399 = 0;
  *(uint8_t*)0x2000039a = 1;
  *(uint8_t*)0x2000039b = 5;
  *(uint8_t*)0x2000039c = 0x24;
  *(uint8_t*)0x2000039d = 0;
  *(uint16_t*)0x2000039e = 0x800;
  *(uint8_t*)0x200003a0 = 0xd;
  *(uint8_t*)0x200003a1 = 0x24;
  *(uint8_t*)0x200003a2 = 0xf;
  *(uint8_t*)0x200003a3 = 1;
  *(uint32_t*)0x200003a4 = 1;
  *(uint16_t*)0x200003a8 = 0x8001;
  *(uint16_t*)0x200003aa = 0x8000;
  *(uint8_t*)0x200003ac = 3;
  *(uint8_t*)0x200003ad = 6;
  *(uint8_t*)0x200003ae = 0x24;
  *(uint8_t*)0x200003af = 0x1a;
  *(uint16_t*)0x200003b0 = 7;
  *(uint8_t*)0x200003b2 = 3;
  *(uint8_t*)0x200003b3 = 5;
  *(uint8_t*)0x200003b4 = 0x24;
  *(uint8_t*)0x200003b5 = 1;
  *(uint8_t*)0x200003b6 = 2;
  *(uint8_t*)0x200003b7 = -1;
  *(uint8_t*)0x200003b8 = 9;
  *(uint8_t*)0x200003b9 = 4;
  *(uint8_t*)0x200003ba = 0x88;
  *(uint8_t*)0x200003bb = 0x4f;
  *(uint8_t*)0x200003bc = 0;
  *(uint8_t*)0x200003bd = 0x79;
  *(uint8_t*)0x200003be = 0x8d;
  *(uint8_t*)0x200003bf = 1;
  *(uint8_t*)0x200003c0 = 0x3f;
  *(uint8_t*)0x200003c1 = 9;
  *(uint8_t*)0x200003c2 = 4;
  *(uint8_t*)0x200003c3 = 0x97;
  *(uint8_t*)0x200003c4 = 0;
  *(uint8_t*)0x200003c5 = 8;
  *(uint8_t*)0x200003c6 = -1;
  *(uint8_t*)0x200003c7 = -1;
  *(uint8_t*)0x200003c8 = -1;
  *(uint8_t*)0x200003c9 = 0x3b;
  *(uint8_t*)0x200003ca = 2;
  *(uint8_t*)0x200003cb = 0x13;
  *(uint8_t*)0x200003cc = 9;
  *(uint8_t*)0x200003cd = 5;
  *(uint8_t*)0x200003ce = 0xe;
  *(uint8_t*)0x200003cf = 0;
  *(uint16_t*)0x200003d0 = 0x200;
  *(uint8_t*)0x200003d2 = 1;
  *(uint8_t*)0x200003d3 = 2;
  *(uint8_t*)0x200003d4 = 5;
  *(uint8_t*)0x200003d5 = 7;
  *(uint8_t*)0x200003d6 = 0x25;
  *(uint8_t*)0x200003d7 = 1;
  *(uint8_t*)0x200003d8 = 3;
  *(uint8_t*)0x200003d9 = 0x3f;
  *(uint16_t*)0x200003da = 0;
  *(uint8_t*)0x200003dc = 9;
  *(uint8_t*)0x200003dd = 5;
  *(uint8_t*)0x200003de = 0x80;
  *(uint8_t*)0x200003df = 0x10;
  *(uint16_t*)0x200003e0 = 0x200;
  *(uint8_t*)0x200003e2 = 0;
  *(uint8_t*)0x200003e3 = 1;
  *(uint8_t*)0x200003e4 = 9;
  *(uint8_t*)0x200003e5 = 9;
  *(uint8_t*)0x200003e6 = 5;
  *(uint8_t*)0x200003e7 = 0;
  *(uint8_t*)0x200003e8 = 0x10;
  *(uint16_t*)0x200003e9 = 0x10;
  *(uint8_t*)0x200003eb = 0x20;
  *(uint8_t*)0x200003ec = -1;
  *(uint8_t*)0x200003ed = 0x80;
  *(uint8_t*)0x200003ee = 2;
  *(uint8_t*)0x200003ef = 0xa;
  *(uint8_t*)0x200003f0 = 2;
  *(uint8_t*)0x200003f1 = 6;
  *(uint8_t*)0x200003f2 = 9;
  *(uint8_t*)0x200003f3 = 5;
  *(uint8_t*)0x200003f4 = 3;
  *(uint8_t*)0x200003f5 = 4;
  *(uint16_t*)0x200003f6 = 8;
  *(uint8_t*)0x200003f8 = 0x3f;
  *(uint8_t*)0x200003f9 = 5;
  *(uint8_t*)0x200003fa = 0x7f;
  *(uint8_t*)0x200003fb = 9;
  *(uint8_t*)0x200003fc = 5;
  *(uint8_t*)0x200003fd = 6;
  *(uint8_t*)0x200003fe = 0;
  *(uint16_t*)0x200003ff = 0x3ff;
  *(uint8_t*)0x20000401 = 0xee;
  *(uint8_t*)0x20000402 = -1;
  *(uint8_t*)0x20000403 = 0x1f;
  *(uint8_t*)0x20000404 = 7;
  *(uint8_t*)0x20000405 = 0x25;
  *(uint8_t*)0x20000406 = 1;
  *(uint8_t*)0x20000407 = 0x80;
  *(uint8_t*)0x20000408 = 5;
  *(uint16_t*)0x20000409 = 3;
  *(uint8_t*)0x2000040b = 9;
  *(uint8_t*)0x2000040c = 5;
  *(uint8_t*)0x2000040d = 0xd;
  *(uint8_t*)0x2000040e = 0xc;
  *(uint16_t*)0x2000040f = 0x20;
  *(uint8_t*)0x20000411 = 0x88;
  *(uint8_t*)0x20000412 = 0x1f;
  *(uint8_t*)0x20000413 = 6;
  *(uint8_t*)0x20000414 = 9;
  *(uint8_t*)0x20000415 = 5;
  *(uint8_t*)0x20000416 = 0;
  *(uint8_t*)0x20000417 = 0;
  *(uint16_t*)0x20000418 = 0x20;
  *(uint8_t*)0x2000041a = 5;
  *(uint8_t*)0x2000041b = 9;
  *(uint8_t*)0x2000041c = 2;
  *(uint8_t*)0x2000041d = 9;
  *(uint8_t*)0x2000041e = 5;
  *(uint8_t*)0x2000041f = 0x80;
  *(uint8_t*)0x20000420 = 4;
  *(uint16_t*)0x20000421 = 0x20;
  *(uint8_t*)0x20000423 = 9;
  *(uint8_t*)0x20000424 = 0xf7;
  *(uint8_t*)0x20000425 = 8;
  *(uint8_t*)0x20000426 = 2;
  *(uint8_t*)0x20000427 = 0xf;
  *(uint8_t*)0x20000428 = 9;
  *(uint8_t*)0x20000429 = 4;
  *(uint8_t*)0x2000042a = 0;
  *(uint8_t*)0x2000042b = 0xac;
  *(uint8_t*)0x2000042c = 2;
  *(uint8_t*)0x2000042d = 0xc8;
  *(uint8_t*)0x2000042e = 0x5e;
  *(uint8_t*)0x2000042f = 0x65;
  *(uint8_t*)0x20000430 = 0x3b;
  *(uint8_t*)0x20000431 = 9;
  *(uint8_t*)0x20000432 = 5;
  *(uint8_t*)0x20000433 = 0xd;
  *(uint8_t*)0x20000434 = 0x10;
  *(uint16_t*)0x20000435 = 0x40;
  *(uint8_t*)0x20000437 = 0xf5;
  *(uint8_t*)0x20000438 = 0xbe;
  *(uint8_t*)0x20000439 = 0x70;
  *(uint8_t*)0x2000043a = 2;
  *(uint8_t*)0x2000043b = 0x21;
  *(uint8_t*)0x2000043c = 9;
  *(uint8_t*)0x2000043d = 5;
  *(uint8_t*)0x2000043e = 0;
  *(uint8_t*)0x2000043f = 0x10;
  *(uint16_t*)0x20000440 = 0x10;
  *(uint8_t*)0x20000442 = 1;
  *(uint8_t*)0x20000443 = 8;
  *(uint8_t*)0x20000444 = 1;
  *(uint32_t*)0x200007c0 = 0;
  *(uint64_t*)0x200007c4 = 0;
  *(uint32_t*)0x200007cc = 0;
  *(uint64_t*)0x200007d0 = 0;
  *(uint32_t*)0x200007d8 = 1;
  *(uint32_t*)0x200007dc = 0;
  *(uint64_t*)0x200007e0 = 0;
  syz_usb_connect(3, 0x105, 0x20000340, 0x200007c0);
}
int main(void)
{
  syscall(__NR_mmap, 0x1ffff000ul, 0x1000ul, 0ul, 0x32ul, -1, 0ul);
  syscall(__NR_mmap, 0x20000000ul, 0x1000000ul, 7ul, 0x32ul, -1, 0ul);
  syscall(__NR_mmap, 0x21000000ul, 0x1000ul, 0ul, 0x32ul, -1, 0ul);
  loop();
  return 0;
}
