// WARNING in __snd_rawmidi_transmit_ack (2)
// https://syzkaller.appspot.com/bug?id=5e794587e5bb5e30cf1458538768c0800209de5e
// status:fixed
// autogenerated by syzkaller (https://github.com/google/syzkaller)

#define _GNU_SOURCE

#include <dirent.h>
#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <signal.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/prctl.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <sys/wait.h>
#include <time.h>
#include <unistd.h>

unsigned long long procid;

static void sleep_ms(uint64_t ms)
{
  usleep(ms * 1000);
}

static uint64_t current_time_ms()
{
  struct timespec ts;
  if (clock_gettime(CLOCK_MONOTONIC, &ts))
    exit(1);
  return (uint64_t)ts.tv_sec * 1000 + (uint64_t)ts.tv_nsec / 1000000;
}

static long syz_open_dev(long a0, long a1, long a2)
{
  if (a0 == 0xc || a0 == 0xb) {
    char buf[128];
    sprintf(buf, "/dev/%s/%d:%d", a0 == 0xc ? "char" : "block", (uint8_t)a1,
            (uint8_t)a2);
    return open(buf, O_RDWR, 0);
  } else {
    char buf[1024];
    char* hash;
    strncpy(buf, (char*)a0, sizeof(buf) - 1);
    buf[sizeof(buf) - 1] = 0;
    while ((hash = strchr(buf, '#'))) {
      *hash = '0' + (char)(a1 % 10);
      a1 /= 10;
    }
    return open(buf, a2, 0);
  }
}

static void kill_and_wait(int pid, int* status)
{
  kill(-pid, SIGKILL);
  kill(pid, SIGKILL);
  int i;
  for (i = 0; i < 100; i++) {
    if (waitpid(-1, status, WNOHANG | __WALL) == pid)
      return;
    usleep(1000);
  }
  DIR* dir = opendir("/sys/fs/fuse/connections");
  if (dir) {
    for (;;) {
      struct dirent* ent = readdir(dir);
      if (!ent)
        break;
      if (strcmp(ent->d_name, ".") == 0 || strcmp(ent->d_name, "..") == 0)
        continue;
      char abort[300];
      snprintf(abort, sizeof(abort), "/sys/fs/fuse/connections/%s/abort",
               ent->d_name);
      int fd = open(abort, O_WRONLY);
      if (fd == -1) {
        continue;
      }
      if (write(fd, abort, 1) < 0) {
      }
      close(fd);
    }
    closedir(dir);
  } else {
  }
  while (waitpid(-1, status, __WALL) != pid) {
  }
}

#define SYZ_HAVE_SETUP_TEST 1
static void setup_test()
{
  prctl(PR_SET_PDEATHSIG, SIGKILL, 0, 0, 0);
  setpgrp();
}

#define SYZ_HAVE_RESET_TEST 1
static void reset_test()
{
  int fd;
  for (fd = 3; fd < 30; fd++)
    close(fd);
}

static void execute_one();

#define WAIT_FLAGS __WALL

static void loop()
{
  int iter;
  for (iter = 0;; iter++) {
    int pid = fork();
    if (pid < 0)
      exit(1);
    if (pid == 0) {
      setup_test();
      execute_one();
      reset_test();
      exit(0);
    }
    int status = 0;
    uint64_t start = current_time_ms();
    for (;;) {
      if (waitpid(-1, &status, WNOHANG | WAIT_FLAGS) == pid)
        break;
      sleep_ms(1);
      if (current_time_ms() - start < 5 * 1000)
        continue;
      kill_and_wait(pid, &status);
      break;
    }
  }
}

uint64_t r[1] = {0xffffffffffffffff};

void execute_one()
{
  long res = 0;
  memcpy((void*)0x20000080, "/dev/dmmidi#", 13);
  res = syz_open_dev(0x20000080, 0x34, 1);
  if (res != -1)
    r[0] = res;
  memcpy((void*)0x200002c0,
         "\x2a\x00\x00\x00\x04\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x03"
         "\x00\x00\x00\x00\x00\x00\x00\x02\x00\x00\x00\x00\x00\x00\x00\x00\x00"
         "\x00\x00\x00\x00\x00\x00\x00\x00\xb4\x8e\x92\x5d\x4b\xfd\xed\x33\x9b"
         "\x2f\x58\x0d\x1f\x3b\x86\x4b\x70\xe0\xc0\x99\xad\x88\x23\xd8\xa4\x4c"
         "\x0c\x6a\xcf\x51\x56\x33\x22\x75\xcf\xaf\xcc\xdc\xe2\xc6\x15\x53\x01"
         "\xc3\xc8\xa5\x54\x27\x48\xa5\x30\xbf\xc1\x3d\x1f\x29\xf8\x32\xe6\x68"
         "\xbf\x83\x72\x2f\xd5\xcd\x41\x55\x1c\x62\x44\x5c\xfb\xf9\x71\xe2\xf7"
         "\xb2\x36\x2a\x85\x5f\xa5\x2e\x1b\x3c\xcd\x70\x41\xab\xbe\x76\x6a\xff"
         "\x11\x07\x75\xfa\x4c\x3e\x32\xe3\x26\xc4\x01\x17\x03\xcc\x8a\x83\x6e"
         "\xfd\x82\xce\xd0\x6d\x7e\xff\x77\xff\x0c\x89\xda\xf1\x70\x52\x12\xf7"
         "\xe3\x98\x5b\xcc\x7d\xb3\x4f\xa0\x16\xee\xfb\x7c\x53\x60\x8b\x8c\x7e"
         "\x02\x36\xad\xca\xe0\x0b\x83\x1b\x38\x12\x1b\x13\x7a\xb9\x8b\xc4\x4e"
         "\x08\xe3\x06\x3b\xb7\xa7\xba",
         211);
  syscall(__NR_write, r[0], 0x200002c0, 0xd3);
  syscall(__NR_ioctl, r[0], 0x2000000040045730, 0x20000000);
}
int main()
{
  syscall(__NR_mmap, 0x20000000, 0x1000000, 3, 0x32, -1, 0);
  for (procid = 0; procid < 8; procid++) {
    if (fork() == 0) {
      loop();
    }
  }
  sleep(1000000);
  return 0;
}
