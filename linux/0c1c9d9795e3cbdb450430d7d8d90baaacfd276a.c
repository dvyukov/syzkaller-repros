// KASAN: slab-out-of-bounds Write in hid_report_raw_event
// https://syzkaller.appspot.com/bug?id=0c1c9d9795e3cbdb450430d7d8d90baaacfd276a
// status:dup
// autogenerated by syzkaller (https://github.com/google/syzkaller)

#define _GNU_SOURCE

#include <endian.h>
#include <errno.h>
#include <fcntl.h>
#include <stdarg.h>
#include <stdbool.h>
#include <stddef.h>
#include <stdint.h>
#include <stdio.h>
#include <stdlib.h>
#include <string.h>
#include <sys/mount.h>
#include <sys/stat.h>
#include <sys/syscall.h>
#include <sys/types.h>
#include <unistd.h>

#include <linux/usb/ch9.h>

unsigned long long procid;

static void sleep_ms(uint64_t ms)
{
  usleep(ms * 1000);
}

#define BITMASK(bf_off, bf_len) (((1ull << (bf_len)) - 1) << (bf_off))
#define STORE_BY_BITMASK(type, htobe, addr, val, bf_off, bf_len)               \
  *(type*)(addr) =                                                             \
      htobe((htobe(*(type*)(addr)) & ~BITMASK((bf_off), (bf_len))) |           \
            (((type)(val) << (bf_off)) & BITMASK((bf_off), (bf_len))))

#define USB_DEBUG 0

#define USB_MAX_EP_NUM 32

struct usb_device_index {
  struct usb_device_descriptor* dev;
  struct usb_config_descriptor* config;
  unsigned config_length;
  struct usb_interface_descriptor* iface;
  struct usb_endpoint_descriptor* eps[USB_MAX_EP_NUM];
  unsigned eps_num;
};

static bool parse_usb_descriptor(char* buffer, size_t length,
                                 struct usb_device_index* index)
{
  if (length <
      sizeof(*index->dev) + sizeof(*index->config) + sizeof(*index->iface))
    return false;
  index->dev = (struct usb_device_descriptor*)buffer;
  index->config = (struct usb_config_descriptor*)(buffer + sizeof(*index->dev));
  index->config_length = length - sizeof(*index->dev);
  index->iface =
      (struct usb_interface_descriptor*)(buffer + sizeof(*index->dev) +
                                         sizeof(*index->config));
  index->eps_num = 0;
  size_t offset = 0;
  while (true) {
    if (offset + 1 >= length)
      break;
    uint8_t desc_length = buffer[offset];
    uint8_t desc_type = buffer[offset + 1];
    if (desc_length <= 2)
      break;
    if (offset + desc_length > length)
      break;
    if (desc_type == USB_DT_ENDPOINT) {
      index->eps[index->eps_num] =
          (struct usb_endpoint_descriptor*)(buffer + offset);
      index->eps_num++;
    }
    if (index->eps_num == USB_MAX_EP_NUM)
      break;
    offset += desc_length;
  }
  return true;
}

enum usb_fuzzer_event_type {
  USB_FUZZER_EVENT_INVALID,
  USB_FUZZER_EVENT_CONNECT,
  USB_FUZZER_EVENT_DISCONNECT,
  USB_FUZZER_EVENT_SUSPEND,
  USB_FUZZER_EVENT_RESUME,
  USB_FUZZER_EVENT_CONTROL,
};

struct usb_fuzzer_event {
  uint32_t type;
  uint32_t length;
  char data[0];
};

struct usb_fuzzer_init {
  uint64_t speed;
  const char* driver_name;
  const char* device_name;
};

struct usb_fuzzer_ep_io {
  uint16_t ep;
  uint16_t flags;
  uint32_t length;
  char data[0];
};

#define USB_FUZZER_IOCTL_INIT _IOW('U', 0, struct usb_fuzzer_init)
#define USB_FUZZER_IOCTL_RUN _IO('U', 1)
#define USB_FUZZER_IOCTL_EVENT_FETCH _IOR('U', 2, struct usb_fuzzer_event)
#define USB_FUZZER_IOCTL_EP0_WRITE _IOW('U', 3, struct usb_fuzzer_ep_io)
#define USB_FUZZER_IOCTL_EP0_READ _IOWR('U', 4, struct usb_fuzzer_ep_io)
#define USB_FUZZER_IOCTL_EP_ENABLE _IOW('U', 5, struct usb_endpoint_descriptor)
#define USB_FUZZER_IOCTL_EP_WRITE _IOW('U', 7, struct usb_fuzzer_ep_io)
#define USB_FUZZER_IOCTL_EP_READ _IOWR('U', 8, struct usb_fuzzer_ep_io)
#define USB_FUZZER_IOCTL_CONFIGURE _IO('U', 9)
#define USB_FUZZER_IOCTL_VBUS_DRAW _IOW('U', 10, uint32_t)

int usb_fuzzer_open()
{
  return open("/sys/kernel/debug/usb-fuzzer", O_RDWR);
}

int usb_fuzzer_init(int fd, uint32_t speed, const char* driver,
                    const char* device)
{
  struct usb_fuzzer_init arg;
  arg.speed = speed;
  arg.driver_name = driver;
  arg.device_name = device;
  return ioctl(fd, USB_FUZZER_IOCTL_INIT, &arg);
}

int usb_fuzzer_run(int fd)
{
  return ioctl(fd, USB_FUZZER_IOCTL_RUN, 0);
}

int usb_fuzzer_event_fetch(int fd, struct usb_fuzzer_event* event)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EVENT_FETCH, event);
}

int usb_fuzzer_ep0_write(int fd, struct usb_fuzzer_ep_io* io)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EP0_WRITE, io);
}

int usb_fuzzer_ep0_read(int fd, struct usb_fuzzer_ep_io* io)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EP0_READ, io);
}

int usb_fuzzer_ep_write(int fd, struct usb_fuzzer_ep_io* io)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EP_WRITE, io);
}

int usb_fuzzer_ep_read(int fd, struct usb_fuzzer_ep_io* io)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EP_READ, io);
}

int usb_fuzzer_ep_enable(int fd, struct usb_endpoint_descriptor* desc)
{
  return ioctl(fd, USB_FUZZER_IOCTL_EP_ENABLE, desc);
}

int usb_fuzzer_configure(int fd)
{
  return ioctl(fd, USB_FUZZER_IOCTL_CONFIGURE, 0);
}

int usb_fuzzer_vbus_draw(int fd, uint32_t power)
{
  return ioctl(fd, USB_FUZZER_IOCTL_VBUS_DRAW, power);
}

#define USB_MAX_PACKET_SIZE 1024

struct usb_fuzzer_control_event {
  struct usb_fuzzer_event inner;
  struct usb_ctrlrequest ctrl;
  char data[USB_MAX_PACKET_SIZE];
};

struct usb_fuzzer_ep_io_data {
  struct usb_fuzzer_ep_io inner;
  char data[USB_MAX_PACKET_SIZE];
};

struct vusb_connect_string_descriptor {
  uint32_t len;
  char* str;
} __attribute__((packed));

struct vusb_connect_descriptors {
  uint32_t qual_len;
  char* qual;
  uint32_t bos_len;
  char* bos;
  uint32_t strs_len;
  struct vusb_connect_string_descriptor strs[0];
} __attribute__((packed));

static const char* default_string = "syzkaller";

static bool lookup_connect_response(struct vusb_connect_descriptors* descs,
                                    struct usb_device_index* index,
                                    struct usb_ctrlrequest* ctrl,
                                    char** response_data,
                                    uint32_t* response_length)
{
  uint8_t str_idx;
  switch (ctrl->bRequestType & USB_TYPE_MASK) {
  case USB_TYPE_STANDARD:
    switch (ctrl->bRequest) {
    case USB_REQ_GET_DESCRIPTOR:
      switch (ctrl->wValue >> 8) {
      case USB_DT_DEVICE:
        *response_data = (char*)index->dev;
        *response_length = sizeof(*index->dev);
        return true;
      case USB_DT_CONFIG:
        *response_data = (char*)index->config;
        *response_length = index->config_length;
        return true;
      case USB_DT_STRING:
        str_idx = (uint8_t)ctrl->wValue;
        if (str_idx >= descs->strs_len) {
          *response_data = (char*)default_string;
          *response_length = strlen(default_string);
        } else {
          *response_data = descs->strs[str_idx].str;
          *response_length = descs->strs[str_idx].len;
        }
        return true;
      case USB_DT_BOS:
        *response_data = descs->bos;
        *response_length = descs->bos_len;
        return true;
      case USB_DT_DEVICE_QUALIFIER:
        *response_data = descs->qual;
        *response_length = descs->qual_len;
        return true;
      default:
        exit(1);
        return false;
      }
      break;
    default:
      exit(1);
      return false;
    }
    break;
  default:
    exit(1);
    return false;
  }
  return false;
}

static volatile long syz_usb_connect(volatile long a0, volatile long a1,
                                     volatile long a2, volatile long a3)
{
  uint64_t speed = a0;
  uint64_t dev_len = a1;
  char* dev = (char*)a2;
  struct vusb_connect_descriptors* descs = (struct vusb_connect_descriptors*)a3;
  if (!dev) {
    return -1;
  }
  struct usb_device_index index;
  memset(&index, 0, sizeof(index));
  int rv = 0;
  rv = parse_usb_descriptor(dev, dev_len, &index);
  if (!rv) {
    return rv;
  }
  int fd = usb_fuzzer_open();
  if (fd < 0) {
    return fd;
  }
  char device[32];
  sprintf(&device[0], "dummy_udc.%llu", procid);
  rv = usb_fuzzer_init(fd, speed, "dummy_udc", &device[0]);
  if (rv < 0) {
    return rv;
  }
  rv = usb_fuzzer_run(fd);
  if (rv < 0) {
    return rv;
  }
  bool done = false;
  while (!done) {
    struct usb_fuzzer_control_event event;
    event.inner.type = 0;
    event.inner.length = sizeof(event.ctrl);
    rv = usb_fuzzer_event_fetch(fd, (struct usb_fuzzer_event*)&event);
    if (rv < 0) {
      return rv;
    }
    if (event.inner.type != USB_FUZZER_EVENT_CONTROL)
      continue;
    bool response_found = false;
    char* response_data = NULL;
    uint32_t response_length = 0;
    if (event.ctrl.bRequestType & USB_DIR_IN) {
      response_found = lookup_connect_response(
          descs, &index, &event.ctrl, &response_data, &response_length);
      if (!response_found) {
        return -1;
      }
    } else {
      if ((event.ctrl.bRequestType & USB_TYPE_MASK) != USB_TYPE_STANDARD ||
          event.ctrl.bRequest != USB_REQ_SET_CONFIGURATION) {
        exit(1);
        return -1;
      }
      done = true;
    }
    if (done) {
      rv = usb_fuzzer_vbus_draw(fd, index.config->bMaxPower);
      if (rv < 0) {
        return rv;
      }
      rv = usb_fuzzer_configure(fd);
      if (rv < 0) {
        return rv;
      }
      unsigned ep;
      for (ep = 0; ep < index.eps_num; ep++) {
        rv = usb_fuzzer_ep_enable(fd, index.eps[ep]);
        if (rv < 0) {
        } else {
        }
      }
    }
    struct usb_fuzzer_ep_io_data response;
    response.inner.ep = 0;
    response.inner.flags = 0;
    if (response_length > sizeof(response.data))
      response_length = 0;
    if (event.ctrl.wLength < response_length)
      response_length = event.ctrl.wLength;
    response.inner.length = response_length;
    if (response_data)
      memcpy(&response.data[0], response_data, response_length);
    else
      memset(&response.data[0], 0, response_length);
    if (event.ctrl.bRequestType & USB_DIR_IN) {
      rv = usb_fuzzer_ep0_write(fd, (struct usb_fuzzer_ep_io*)&response);
    } else {
      rv = usb_fuzzer_ep0_read(fd, (struct usb_fuzzer_ep_io*)&response);
    }
    if (rv < 0) {
      return rv;
    }
  }
  sleep_ms(200);
  return fd;
}

struct vusb_descriptor {
  uint8_t req_type;
  uint8_t desc_type;
  uint32_t len;
  char data[0];
} __attribute__((packed));

struct vusb_descriptors {
  uint32_t len;
  struct vusb_descriptor* generic;
  struct vusb_descriptor* descs[0];
} __attribute__((packed));

struct vusb_response {
  uint8_t type;
  uint8_t req;
  uint32_t len;
  char data[0];
} __attribute__((packed));

struct vusb_responses {
  uint32_t len;
  struct vusb_response* generic;
  struct vusb_response* resps[0];
} __attribute__((packed));

static bool lookup_control_response(struct vusb_descriptors* descs,
                                    struct vusb_responses* resps,
                                    struct usb_ctrlrequest* ctrl,
                                    char** response_data,
                                    uint32_t* response_length)
{
  int descs_num = (descs->len - offsetof(struct vusb_descriptors, descs)) /
                  sizeof(descs->descs[0]);
  int resps_num = (resps->len - offsetof(struct vusb_responses, resps)) /
                  sizeof(resps->resps[0]);
  uint8_t req = ctrl->bRequest;
  uint8_t req_type = ctrl->bRequestType & USB_TYPE_MASK;
  uint8_t desc_type = ctrl->wValue >> 8;
  if (req == USB_REQ_GET_DESCRIPTOR) {
    int i;
    for (i = 0; i < descs_num; i++) {
      struct vusb_descriptor* desc = descs->descs[i];
      if (!desc)
        continue;
      if (desc->req_type == req_type && desc->desc_type == desc_type) {
        *response_length = desc->len;
        if (*response_length != 0)
          *response_data = &desc->data[0];
        else
          *response_data = NULL;
        return true;
      }
    }
    if (descs->generic) {
      *response_data = &descs->generic->data[0];
      *response_length = descs->generic->len;
      return true;
    }
  } else {
    int i;
    for (i = 0; i < resps_num; i++) {
      struct vusb_response* resp = resps->resps[i];
      if (!resp)
        continue;
      if (resp->type == req_type && resp->req == req) {
        *response_length = resp->len;
        if (*response_length != 0)
          *response_data = &resp->data[0];
        else
          *response_data = NULL;
        return true;
      }
    }
    if (resps->generic) {
      *response_data = &resps->generic->data[0];
      *response_length = resps->generic->len;
      return true;
    }
  }
  return false;
}

static volatile long syz_usb_control_io(volatile long a0, volatile long a1,
                                        volatile long a2)
{
  int fd = a0;
  struct vusb_descriptors* descs = (struct vusb_descriptors*)a1;
  struct vusb_responses* resps = (struct vusb_responses*)a2;
  struct usb_fuzzer_control_event event;
  event.inner.type = 0;
  event.inner.length = USB_MAX_PACKET_SIZE;
  int rv = usb_fuzzer_event_fetch(fd, (struct usb_fuzzer_event*)&event);
  if (rv < 0) {
    return rv;
  }
  if (event.inner.type != USB_FUZZER_EVENT_CONTROL) {
    return -1;
  }
  bool response_found = false;
  char* response_data = NULL;
  uint32_t response_length = 0;
  if (event.ctrl.bRequestType & USB_DIR_IN) {
    response_found = lookup_control_response(descs, resps, &event.ctrl,
                                             &response_data, &response_length);
    if (!response_found) {
      return -1;
    }
  } else {
    response_length = event.ctrl.wLength;
  }
  struct usb_fuzzer_ep_io_data response;
  response.inner.ep = 0;
  response.inner.flags = 0;
  if (response_length > sizeof(response.data))
    response_length = 0;
  if (event.ctrl.wLength < response_length)
    response_length = event.ctrl.wLength;
  response.inner.length = response_length;
  if (response_data)
    memcpy(&response.data[0], response_data, response_length);
  else
    memset(&response.data[0], 0, response_length);
  if (event.ctrl.bRequestType & USB_DIR_IN) {
    rv = usb_fuzzer_ep0_write(fd, (struct usb_fuzzer_ep_io*)&response);
  } else {
    rv = usb_fuzzer_ep0_read(fd, (struct usb_fuzzer_ep_io*)&response);
  }
  if (rv < 0) {
    return rv;
  }
  sleep_ms(200);
  return 0;
}

static volatile long syz_usb_ep_write(volatile long a0, volatile long a1,
                                      volatile long a2, volatile long a3)
{
  int fd = a0;
  uint16_t ep = a1;
  uint32_t len = a2;
  char* data = (char*)a3;
  struct usb_fuzzer_ep_io_data io_data;
  io_data.inner.ep = ep;
  io_data.inner.flags = 0;
  if (len > sizeof(io_data.data))
    len = sizeof(io_data.data);
  io_data.inner.length = len;
  memcpy(&io_data.data[0], data, len);
  int rv = usb_fuzzer_ep_write(fd, (struct usb_fuzzer_ep_io*)&io_data);
  if (rv < 0) {
    return rv;
  }
  sleep_ms(200);
  return 0;
}

uint64_t r[1] = {0xffffffffffffffff};

int main(void)
{
  syscall(__NR_mmap, 0x20000000, 0x1000000, 3, 0x32, -1, 0);
  intptr_t res = 0;
  memcpy(
      (void*)0x20000540,
      "\x12\x01\x00\x00\x00\x00\x00\x20\x6d\x04\x1c\xc7\x40\x00\x00\x00\x00\x01"
      "\x09\x02\x24\x00\x01\x00\x00\xa0\x00\x09\x04\x00\x00\x09\x03\x01\x01\x00"
      "\x09\x21\x00\x00\x00\x01\x22\x15\x00\x09\x05\x81\xd3\xcd\x63\x33\x00\x00"
      "\xee\xb4\x88\x8f\xff\xfc\x1f\xe0\x2d\xa3\x65\xa9\xc4\xce\xfe\x8b\x90\x61"
      "\x70\x1c\x3c\xcd\x35\x2d\x15\x83\x70\x3e\xe6\xa2\xce\x0b\x65\xf5\x4a\x55"
      "\x25\xda\xaa\x9a\x73\x96\xfd\xc4\x9f\x59\x66\xf9\x00\x63\x55\x11\xc5\x62"
      "\x10\x23\x5d\xca\x74\xb0\x46\xbe\x3b\x46\xe5\xb6\x48\x2b\x05\x00\xe1\x8c"
      "\x4a\x00\x00\x00\x00\x9c\xc7\x04\xcc\x79\x15\x55\x97\x94\x80\x5a\x7a\xb1"
      "\x3d\x44\x84\x90\xdb\xac\xf2\x68\xac\x24\x72\x3a\xcf\x3c\xa4\x84\xbe\x4d"
      "\x52\x32\x35\x99\x80\x1b\x61\xa4\xa4\xfd\x48\x44\xf9\xeb\xc1\xd2\xc0\x99"
      "\xd1\x74\x7d\x8a\x91\xb7\x62\xf8\x21\x02\x49\x1b\x72\x36\x20\xa3\xb8\x27"
      "\x0a\x84\xab\x79\x05\xcf\xac\x5f\xbf\xf1\xc3\xb2\x59\x2b\x1c\x7b\xf2\x32"
      "\x64\xe0\x73\x61\x9b\x76\x05\xc4\x6f\xe4\x0f\xa7\xc3\xf5\x04\xd8\xc8\xea"
      "\x34\xf3\xb2\x2e\x92\x00\x00\x00\x00\x00\x00\x00\x00\x00\x00\x0c\xe1\x73"
      "\x50\x57\x5b\x02\xf3\xa6\xce\x86\x5b\xae\x57\x86\x0c\x3a\x03\x47\x95\x04"
      "\xd5\xb3\x18\x12\x5c\x9c\xce\xb9\x78\x42\x48\xb7\x17\xe6\x01\xb8\xa1\x64"
      "\xb1\x1d\xa4\xaf\x34\x64\x2c\x5f\x8e\xa5\xda\x2e\xbd\x68\x98\x42\xb4\x9b"
      "\x41\xcd\x26\x7e\x65\x57\x98\xfb\x41\x48\xae\x4e\x9b\x26\x64\xc7\xb7\x16"
      "\x5d\xed\xaa\x21\x03\x38\xa4\xf0\xd7\x93\xba\x8e\x9c\xf1\xe0\xaf\x97\xe3"
      "\x33\x92\xb3\x37\x64\xb6\xe2\x41\x0b\x20\x80\xdb\x32\x53\x7c\x96\xd5\xb0"
      "\xc9\xcc\x4a\xdc\x0e\xa4\xc2\x13\x58\xc7\x24\xdc\xd9\x65\x75\xa2\x93\xc3"
      "\xd0\x27\xd8\x9f\xb9\x70\xa7\x05\xae\x62\x3f\xb7\x2e\x64\xda\x4f\xf1\xd4"
      "\xd9\x05\x12\xeb\x63\x6e\xa3\xd7\x24\xd9\x88\xcc\xbc\xff\x2f\x74\x9a\x02"
      "\x76\xe6\xd9\xea\x4d\x08\xba\x22\x17\xf9\xe2\x5d\x61\x65\x7c\x71\xec\xbc"
      "\x46\xbb\x94\xff\x5e\x6d\xb2\xcd\xe7\x6d\x32\xab\xc3\x47\xcd\x4a\xa1\x5a"
      "\xdf\x08\x72\xa8\x8c\x20\xfa\x8f\x84\x9b\x58\x58\x86\x26\x09\x19\xbc\xf7"
      "\x35\xa6\x44\x22\x2f\x8e\xf9\xa9\x0f\x60\x63\x2c\xbe\xc9\xdb\xaa\x6b\x9b"
      "\xb8\xa6\x6e\x4f\xb1\x74\xa4\xf0\xbe\xdd\xd9\x5b\x84\x0c\xdb\x17\x6e\x95"
      "\x63\x14\x98\x07\xfd\x04\x52\x5f\x83\x64\xab\xf1\xfc\x0c\x02\x40\xff\xc1"
      "\x12\x7b\x86\x9c\x42\x5f\x56\x6a\x56\xb9\x40\xfc\x80\x7a\xab\xc6\x8c\x12"
      "\x84\x2e\xd8\xc2\x35\xa3\x3b\x72\x2e\x61\x3c\x59\x1a\xf5\xf6\xa8\x6e\xb7"
      "\x95\xb5\x2d\x90\x3b\x45\x9e\x87\x71\xb1\x12\xd4\xe7\xdf\x91\xa2\xf4\x18"
      "\xca\x7d\x25\xde\x12\xfe\x81\xdd\x19\xf9\x26\xbf\xda\x61\x4a\x3a\x42\xf6"
      "\xf8\xca\xac\x9b\xcb\x59\xcb\xf3\x93\xf2\x14\x6f\x1c\x5d\x93\x7f\x60\xfa"
      "\x17\xc9\x70\x61\x24\x92\x11\xab\xfd\x93\x48\xd3\xcb\xa9\xdc\x9f\x82\xdf"
      "\xf6\xd5\x70\x4b\xc4\xca\x3e\xdf\xd3\x7b\x72\xea\x00\x00",
      644);
  res = syz_usb_connect(0, 0x36, 0x20000540, 0);
  if (res != -1)
    r[0] = res;
  syz_usb_control_io(r[0], 0, 0);
  *(uint32_t*)0x20000080 = 0x24;
  *(uint64_t*)0x20000084 = 0;
  *(uint64_t*)0x2000008c = 0;
  *(uint64_t*)0x20000094 = 0x20000040;
  *(uint8_t*)0x20000040 = 0;
  *(uint8_t*)0x20000041 = 0x22;
  *(uint32_t*)0x20000042 = 0x15;
  STORE_BY_BITMASK(uint8_t, , 0x20000046, 0, 0, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000046, 0, 2, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000046, 0, 4, 4);
  STORE_BY_BITMASK(uint8_t, , 0x20000047, 3, 0, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000047, 1, 2, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000047, 0, 4, 4);
  memcpy((void*)0x20000048, "\x38\x5c\xb5\x9b", 4);
  STORE_BY_BITMASK(uint8_t, , 0x2000004c, 3, 0, 2);
  STORE_BY_BITMASK(uint8_t, , 0x2000004c, 2, 2, 2);
  STORE_BY_BITMASK(uint8_t, , 0x2000004c, 0, 4, 4);
  memcpy((void*)0x2000004d, "\xeb\xea\xf2\xa2", 4);
  STORE_BY_BITMASK(uint8_t, , 0x20000051, 1, 0, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000051, 1, 2, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000051, 8, 4, 4);
  memcpy((void*)0x20000052, "R", 1);
  STORE_BY_BITMASK(uint8_t, , 0x20000053, 2, 0, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000053, 0, 2, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000053, 8, 4, 4);
  memcpy((void*)0x20000054, "\xd9\x76", 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000056, 3, 0, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000056, 0, 2, 2);
  STORE_BY_BITMASK(uint8_t, , 0x20000056, 0, 4, 4);
  memcpy((void*)0x20000057, "\x02\xbf\x77\x31", 4);
  *(uint64_t*)0x2000009c = 0;
  *(uint32_t*)0x200004c0 = 0x1f;
  *(uint64_t*)0x200004c4 = 0;
  *(uint64_t*)0x200004cc = 0;
  *(uint64_t*)0x200004d4 = 0;
  *(uint64_t*)0x200004dc = 0;
  *(uint64_t*)0x200004e4 = 0;
  syz_usb_control_io(r[0], 0x20000080, 0x200004c0);
  memcpy((void*)0x20000140,
         "\x52\x8f\x2e\x42\x43\xac\x30\x58\xce\xe1\x24\xb0\x5a\xe6\xcc\x6b\x55"
         "\x8a\x74\x78\x20\xc4\x49\x3e\xff\xe1\xd3\x65\x56\xce\x66\xd5\xb4\xf0"
         "\xa9\x59\xd2\xd1\xe8\x1b\x2d\xc2\x06\x5c\x5e\x5a\x3e\x92\x04\xaa\xd9"
         "\xea\x11\x96\x26\xab\x90\x4a\xd7\x23\xf2\xd9\x64\xc8\xc3\xb3\xaf\xb4"
         "\x7b\x8b\x72\x62\x77\x31\x34\x29\x4a\x90\xac\xc7\xcd\x62\xa6\x2d\x02"
         "\x06\x1a\xdd\x1d\xc6\x31\xb6\x0b\xfd\x91\x82\x50\xc7\x4e\x04\xf8\x61"
         "\x35\xbd\xf8\x1e\x8e\x8e\x11\x91\x82\x03\x13\x9d\xac\x13\xdf",
         117);
  syz_usb_ep_write(r[0], 0, 0x75, 0x20000140);
  return 0;
}
